import { PASTEL } from "../config";
import { fbGet, fbSet } from "../firebase";
import { dbGetAllNotes } from "../lib/wrongNoteDB";

// --- Profanity Filter (Smart Detection) ---

// Base words - the core forms
const BASE_PROFANITY = [
  // 한국어 핵심 욕설
  "시발","씨발","씨빨","시빨","씨팔","시팔","시부랄","씨부랄","씨불","시불",
  "씹","씹할","씹새","씹년","씹놈","씹덕","씹물","씹치","씹팔","18","십팔","열여덟",
  "개새끼","개색끼","개새기","개색기","개세끼","개자식","개년","개놈","개돼지","개쓰레기","개소리","개지랄","개씹","개좆",
  "병신","병먹","병딱",
  "지랄","지럴","지랼","지롤",
  "닥쳐","닥치","닥처","입닥쳐",
  "꺼져","꺼저","꺼지세요",
  "미친놈","미친년","미친새끼","미친것",
  "좆","좆같","좆나","좆밥","좆만","좆까",
  "새끼","색끼","쌔끼",
  "썅","쌍놈","쌍년",
  "멍청","찐따","찐다","등신","돌아이","또라이","돌대가리",
  "엿먹","엿같","엿되",
  "느금마","느금","니미","니애미","니애비","느개비",
  "걸레","화냥년","창녀","창남","갈보",
  "죽어","뒤져","뒈져","뒤질","죽여","죽일","뒤져라",
  "꼴값","꼴통","추접","추잡",
  "거지","쪽팔","꼬붕","따까리",
  "빡대가리","대가리","주둥이","주둥아리",
  "쓰레기","찌질","찌질이",
  "한남충","한녀충","맘충","틀딱","급식충","진지충",
  "존나","졸라","존니","졸리",
  "빨갱이","쪽바리","짱깨","짱개",

  // 한국어 외설 핵심
  "섹스","야동","포르노","자위","딸치","딸딸이",
  "보지","자지","음경","성기","가슴만져",
  "강간","성폭행","성추행","성희롱","몰카","도촬",
  "음란","변태","노출증",
  "원조교제","원교","조건만남","파파활",
  "떡치다","떡질","따먹다","따먹어",
  "사까시","사까",
  "벗방","몸캠","폰섹","야캠",
  "섹파","쎅파","원나잇",
  "n번방","박사방","그루밍","성착취",
  "오나홀","텐가","딜도","바이브",
  "정액","사정","질액","애액",
  "발기","귀두","고환","음핵","클리토리스","음순",
  "페니스","바기나",
  "체위","후배위","정상위","여상위",
  "쓰리섬","난교","스와핑","근친상간",
  "소아성애","로리","쇼타",
  "얼싸","몸싸","입싸","중출",
  "딥쓰로트","펠라치오","쿤니",
  "애널","항문성교","피스팅",
  "육봉","육변기","꼴리다","꼴림",
  "야짤","야겜","19금",
  "후장","뒷구멍","똥꼬",
  "대딸","핸플","풀코스",
  "게이비하","호모","레즈비하","트젠비하",

  // 영어
  "fuck","shit","bitch","asshole","bastard","damn","dick","cock","pussy","cunt",
  "whore","slut","retard","nigger","nigga","faggot","fag","motherfucker",
  "wanker","tosser","bollocks","prick","douche","douchebag",
  "penis","vagina","clitoris","testicle","scrotum","erection","boner",
  "ejaculate","masturbate","blowjob","handjob","rimjob","deepthroat",
  "gangbang","orgy","threesome","creampie","facial","squirt",
  "fellatio","cunnilingus","doggystyle","bareback",
  "dominatrix","bondage","spank","choking",
  "pornhub","xvideos","xhamster","onlyfans","chaturbate",
  "stripper","escort","hooker","prostitute","brothel",
  "pedophile","bestiality","necrophilia","incest","jailbait",
  "lolita","loli","shota","upskirt","voyeur","grope",
  "revenge porn","deepfake","sextortion","grooming",
  "horny","kinky","lewd","smut","hentai","ahegao","futanari",
  "sexting","nudes","dickpic",

  // 일본어
  "くそ","クソ","ばか","バカ","馬鹿","あほ","アホ",
  "しね","シネ","死ね","ころす","殺す",
  "きもい","キモい","うざい","ウザい",
  "畜生","くたばれ","野郎","テメエ","貴様",
  "ゴミ","カス","クズ","デブ","ブス","ハゲ",
  "まんこ","マンコ","ちんこ","チンコ","ちんぽ","チンポ",
  "おっぱい","オッパイ","エロ","エッチ","変態",
  "痴漢","ヤリマン","淫乱","セックス","オナニー",
  "レイプ","強姦","売春","風俗","ソープ",
  "ロリコン","ショタコン","キチガイ","障害者","ガイジ",
  "中出し","顔射","ぶっかけ","フェラ","クンニ",
  "手コキ","パイズリ","潮吹き","乱交","緊縛",
  "盗撮","パンチラ","肉便器","アヘ顔","寝取り",

  // 중국어
  "他妈的","操你妈","操你","草你妈","傻逼","傻B",
  "煞笔","白痴","蠢货","笨蛋","混蛋","王八蛋",
  "贱人","贱货","狗日的","狗娘养","畜生",
  "废物","垃圾","妈逼","尼玛","泥马",
  "卧槽","我操","我草","去死","该死","滚蛋",
  "脑残","智障","弱智","屌丝","逼","鸡巴",
  "做爱","啪啪","打炮","约炮","色情","黄片","毛片",
  "妓女","嫖","卖淫","强奸","猥亵","性骚扰",
  "打飞机","撸管","口交","肛交","骑乘","高潮",
  "内射","颜射","群交","换妻","恋童","萝莉控",
  "捆绑","调教","偷拍","偷窥","裸聊",
  "飞机杯","跳蛋","黄漫","骚货","骚穴","肉棒",
  "处女","破处","基佬","搞基","人妖","伪娘",
];

// Korean character substitution maps for auto-detection
const KO_SUBS = {
  "ㅅ": ["ㅆ","s","5"],
  "ㅂ": ["8","b","ㅃ","6"],
  "ㄱ": ["9","ㄲ","g","k"],
  "ㅈ": ["ㅉ","z","j"],
  "ㄷ": ["ㄸ","d"],
  "ㅁ": ["m"],
  "ㄴ": ["n"],
  "ㄹ": ["r","l"],
  "ㅇ": ["0","o"],
  "ㅎ": ["h"],
  "ㅗ": ["0","o"],
  "ㅏ": ["a","ㅑ"],
  "ㅓ": ["e","ㅕ"],
  "ㅣ": ["1","i","l","!"],
  "ㅜ": ["u","ㅠ"],
  "ㅡ": ["-","_"],
  "시": ["씨","ci","si","쉬","ㅅl","ㅆl","sl"],
  "발": ["빨","bal","팔","벌","벨","밸"],
  "병": ["byung","벼응","ㅂㅇ"],
  "신": ["shin","sin","씬","싄"],
  "개": ["gae","게","걔","캐"],
  "새": ["세","쌔","sae"],
  "끼": ["키","기","kki"],
};

// Consonant-only (초성) patterns
const CHOSUNG_MAP = [
  ["ㅅㅂ","ㅆㅂ","ㅅ ㅂ","ㅆ ㅂ"],
  ["ㅂㅅ","ㅃㅅ","ㅂ ㅅ"],
  ["ㅈㄹ","ㅉㄹ","ㅈ ㄹ"],
  ["ㄱㅅㄲ","ㄱㅅ ㄲ"],
  ["ㅅㄲ","ㅆㄲ","ㅅ ㄲ"],
  ["ㅁㅊ","ㅁ ㅊ"],
  ["ㅂㅈ","ㅂ ㅈ"],
  ["ㅈㅈ","ㅉㅉ"],
  ["ㅈㄴ","ㅉㄴ"],
  ["ㄲㅈ","ㄲ ㅈ"],
  ["ㅄ","ㅂㅅ"],
  ["ㅅㅂㄴ","ㅆㅂㄴ"],
  ["ㅅㅂㄹ","ㅆㅂㄹ"],
  ["ㅈㄹㅇ"],
  ["ㄴㄱㅁ"],
  ["ㄷㅊ"],
  ["ㅎㅇ"],
  ["ㅈㅅ"],
  ["ㅈ같","ㅈ까"],
];

// Leet/number/symbol substitution patterns
const LEET_PATTERNS = [
  ["tl발","tlqkf","Tlqkf"],
  ["gkrtod"], // 병신 in keyboard mapping
  ["dkssud"], // 닥쳐 in keyboard mapping
  ["qkfzm"], // 발음 in keyboard mapping
  ["si8","ssi8","c8","s1bal","ss1bal"],
  ["shibal","ssibal","sibal","shibbal"],
  ["byeongsin","byungsin","byungshin"],
  ["gaesaekki","geseki","kaesaekki"],
  ["jiral","jilal","zilar"],
  ["seksu","sekksu"],
  ["f*ck","f**k","fu*k","fvck","phuck","fxck","f u c k"],
  ["sh*t","s*it","sh!t","$hit"],
  ["b*tch","b!tch","bi+ch"],
  ["a$$","a**","a$$hole","a**hole"],
  ["d*ck","d!ck","d1ck"],
  ["p*ssy","pu$$y"],
  ["c*nt","c**t"],
  ["n*gger","n!gger","n1gga"],
  ["wh*re","h0e"],
  ["sl*t","s1ut"],
  ["r*tard","r3tard"],
  ["p0rn","pr0n","p*rn"],
  ["s3x","s*x","$ex"],
];


// Phonetic similarity map - normalizes similar-sounding Korean syllables
const PHONETIC_MAP = {
  // ㅅ/ㅆ 계열
  "씨":"시","쉬":"시","씌":"시","싀":"시","쒸":"시","슈":"시","쓔":"시","씨":"시",
  "빨":"발","벌":"발","밸":"발","뻘":"발","볼":"발","뱔":"발","팔":"발","펄":"발",
  "씹":"십","싶":"십","씝":"십","쉽":"십","숩":"십",
  // ㅂ/ㅃ 계열
  "뼝":"병","벵":"병","볌":"병","뼈":"병","뼉":"병",
  "씬":"신","싄":"신","씐":"신","쓘":"신",
  // ㄱ/ㄲ 계열
  "깨":"개","께":"개","겨":"개","겨":"개",
  "쌔":"새","쎄":"새","셰":"새","쎼":"새",
  "끼":"키","기":"키","끼":"키","키":"키",
  // ㅈ/ㅉ 계열
  "찌":"지","쪼":"조","째":"재","쯔":"즈",
  "쫗":"좆","졷":"좆","좃":"좆","족":"좆",
  // 기타 음운 변형
  "랼":"랄","럴":"랄","롤":"랄","뢀":"랄",
  "넘":"놈","늠":"놈",
  "녀":"년","녜":"년",
  "쳐":"처","쳐":"처",
  "쪄":"져","쪄":"져",
  "미":"미","믜":"미",
  "친":"친","틴":"친",
  "존":"존","쫀":"존","졸":"존",
  "나":"나","내":"나","니":"나",
  "느":"느","늬":"느","눼":"느",
  "금":"금","긍":"금","그":"금",
  "마":"마","매":"마","머":"마",
  "엿":"엿","옛":"엿","엳":"엿",
  "닥":"닥","딱":"닥","딲":"닥",
  "꺼":"꺼","껴":"꺼","거":"꺼",
  "뒤":"뒤","뒈":"뒤","듸":"뒤","뒹":"뒤",
  "쓰":"쓰","스":"쓰","슬":"쓰",
  "떡":"떡","덕":"떡","턱":"떡",
  "박":"박","빡":"박","밖":"박",
  "딸":"딸","달":"딸","탈":"딸",
};

// Phonetic variants - manually expanded common evasion words
const PHONETIC_VARIANTS = [
  // 시발 계열 (모든 발음 변형)
  "시발","씨발","시바","씨바","시방","씨방","시봉","씨봉","시벌","씨벌",
  "시빨","씨빨","시뻘","씨뻘","시불","씨불","시부랄","씨부랄","시부럴",
  "시파","씨파","시펄","씨펄","시팔","씨팔","시퍌","씨퍌",
  "싀발","씌발","쉬발","쒸발","쓔발","슈발",
  "시1발","씨1발","시l발","씨l발","시!발","씨!발",
  "시ㅣ발","씨ㅣ발","시이발","씨이발",
  "시바라","씨바라","시바랄","시발롬","씨발롬","시발놈","씨발놈","시발년","씨발년",
  "시벨","씨벨","시볼","씨볼","시뷀","씨뷀",
  "시1팔","씨1팔","시l팔","씨l팔",
  "C발","c발","si발","ssi발",
  "시foot","씨foot",
  
  // 씹 계열
  "씹","십","싶","씝","쉽","숩","씹할","십할","씹새","십새",
  "씹년","십년","씹놈","십놈","씹팔","십팔","씹창","십창",
  "18","십팔","열여덟","십8","씹8","18놈","18년",
  "씹선비","씹덕","씹치","씹물","씹새끼","십새끼",

  // 병신 계열
  "병신","뼝신","벵신","볌신","병씬","병싄","병씐","병시인",
  "ㅂㅇ신","벼응신","별신","벌신","붱신","뵹신","뼁신",
  "병1신","병|신","byungsin","byeongsin","byungshin",
  "병시","병시이인","벼응시인","별씬",

  // 지랄 계열
  "지랄","지럴","지롤","지뢀","지렬","짓랄","찌랄","쥐랄",
  "G랄","g랄","zi랄","ji랄","지1랄","지l랄",
  "지뢜","지뤌","지뢔",

  // 좆 계열
  "좆","졷","좃","족같","졷같","좃같","좆같","족까","졷까",
  "좆나","족나","졷나","좃나",
  "조또","조또","좃도","졷도",
  "jot","zot",

  // 개새끼 계열
  "개새끼","개색끼","개새기","개색기","개세끼","개세기","개쉐끼",
  "개쌔끼","개쌔기","개쎄끼","개쎄기","개셰끼","개쎼끼",
  "개새키","개색키","개세키","개쌔키","개쎄키",
  "개싀끼","개씌끼","개쒸끼",
  "걔새끼","겤새끼","께새끼","깨새끼",
  "개자식","개자슥","개자시","개쟈식",
  "개넘","개노무","개놈아",

  // 새끼 계열
  "새끼","색끼","세끼","쌔끼","쎄끼","셰끼","쎼끼",
  "새키","색키","세키","쌔키","쎄키",
  "새기","색기","세기","쌔기","쎄기",
  "싀끼","씌끼","쒸끼","쉐끼",
  "saecci","saekki",

  // 존나 계열
  "존나","졸라","존내","졸래","존니","졸리","존너","졸려",
  "쫀나","쬰나","쮼나","즌나","쥰나",
  "존1나","존|나","존ㄴ나",
  "jonna","zonna","jola",

  // 미친 계열
  "미친","미틴","미띤","미씬","밋친","믿친","미친놈","미친년",
  "미친새끼","미친새기","미친색끼","미틴놈","미틴년",
  "ㅁㅊ","ㅁ1ㅊ","미1친",

  // 닥쳐 계열
  "닥쳐","닥치","닥처","딱쳐","딱치","딲쳐",
  "닥치세요","닥쳐라","입닥쳐","입닥","입딲",
  "dak쳐","닥chyeo",

  // 꺼져 계열
  "꺼져","꺼저","거져","껴져","꺼지","거지",
  "꺼져라","꺼져버려","꺼지세요",

  // 느금마 계열
  "느금마","느그마","늑마","느금","느그미","느그매",
  "느금빠","느그빠","늑빠",
  "니미","니미럴","니미랄","니미씨","니미씹",
  "니애미","니애비","느개비","니엄마","니아빠",
  "엄마가","아빠가","부모",

  // 엿 계열
  "엿먹","엿같","엿되","옛먹","엳먹","에먹",
  "엿이나","옛이나","엳이나",

  // 걸레 계열
  "걸레","걸래","걸레년","걸레녀","걸레같",
  "갈보","갈보년","화냥","화냥년",
  "창녀","창남","매춘부","매춘남",

  // 등신 계열
  "등신","등쉰","등씬","등시인","뜽신","듕신",
  "돌아이","도라이","또라이","도래이","돌대가리","돌머리",
  "찐따","찐다","진따","진다","찐빠",
  "멍청","멍청이","멍청아","멍총","먱청",

  // 뒤져 계열
  "뒤져","뒈져","듸져","뒤저","뒤질","뒈질","듸질",
  "뒤져라","뒈져라","뒤질래","뒈질래",
  "죽어","주거","쥬거","죽여","주겨","죽일",
  "자살","자살해","자살해라",

  // 보지/자지 계열
  "보지","보G","보ㅈ","봊이","보즤","보ㅈㅇ","뽀지",
  "자지","자G","자ㅈ","쟈지","쟈ㅈ","좌지",
  
  // 떡/따먹 계열
  "떡치다","떡쳤","떡질","떡실신","뗙치","턱치",
  "따먹다","따먹어","따먹자","따묵","딸먹",
  "박다","박았","박히다","박아","빡다",

  // 섹스 계열  
  "섹스","쎅스","쎡스","섹슈","색스","셱스","세엑스","쎅쓰",
  "sex","seX","sEx","SEX","s3x","$ex","se><",

  // 야동 계열
  "야동","야둥","얃동","야도옹","약동","야동영상",
  "포르노","뽀르노","포르너","폴노","포ooo르노",
  "porn","pr0n","p0rn","pron",

  // 한남/한녀 계열
  "한남충","한남","한녀충","한녀","맘충","틀딱","급식충","진지충",
  "김치녀","김치남","된장녀","된장남",

  // 추가 발음 변형
  "빡대가리","빡대","빡머리","대가리","대갈","대갈빡",
  "주둥이","주둥아리","주뎅이","쥬뎅이",
  "쓰레기","쓰뤠기","쓸레기","쓰래기","스레기",
  "찌질","찌질이","찌찔","찔찔",
  "꼴값","꼴깝","꼬라지","꼬라보다",
  "거지","거지같","거지같은","구걸",

  // ===== Open Source + 발음 유사 추가 (korean-bad-words 기반) =====
  "10JIL","10넘","10놈","10발넘","10발년","10발놈","10발눔","10밸넘","10밸놈","10밸늠","10버지","10새끼","10새리","10쎄끼","10쎈치",
  "10쎝치","10알","10앨넘","10앨놈","10앨늠","10창","10챵","10탱","10탱아","10팔","10팔년","10팔놈","10팔연","10할년","10할연",
  "18넘","18눔","18늠","18새끼","18세끼","18쉑끼","18쎄리","18쒜끼","18쒜리","18쒝끼","18씨끼","2C8","2C팔","4KKASI","ABUSE",
  "AC발","ADULT","ADULTSEXSHOP","ADULTVIDEO","ADULTZONE","ALBA","ANAL","APFHD","AQUABALL","ARB구함","ASIANGIRL","ASS","ASSHOLE","AWOODONG","BACASTV",
  "BACKBOJYTV","BBAGOORI","BBAGURY","BDSM","BEHEADING","BESTBOZI","BINYA","BITCH","BOGY","BOJI","BOJY","BOJYTY","BOZI","BOZICAM","BOZY",
  "BO지","BUNSEC","BUNSEK","BYUNTAE","BYUNTAESEX","C2JOY","C8","C818","C8C8","CARPORNO","CARSEX","CBT","CCIPAL","CCIVAL","CE발",
  "CIBAL","CIPAL","CIVAL","CLUBERO","CLUD","COMESEK","COMSEK","CONDOM","COSEX","COSEX.NET","CUNNILINGUS","C댕","C팔","DABANG","DAMN",
  "DAMOIMSEX","DDUK","DILDO","DOG새끼","DOUMI","D랄","ECSTASY","ERO","ERO2030","ERO69","EROASIA","EROCINE","EROSASIA","EROSIAN","EROSTYLE",
  "FANTADREAM","FEALLATIO","FEMSUB","FETISH","FETISH7","FETISHWOMAN","FUCK","Fuck","GABOZI","GAGASEXY","GANGBANG","GANGSHAQ","GOCINEPIA","GOJASEX","GORE",
  "HADURI","HARDCORE","HARDCOREPORNO","HENTAI","HIDDEN","HOMPY","HOSTMASTER","HOTBOJY","HOTSEX","HOTSEXKOREA","IJSEX","IJ에로쇼","ILOVESEX","INFO","JAJI",
  "JAMJI","JASAL","JAWE","JAWI","JAZI","JAZY","JA위","JA지","JCB","JFANTASY","JGIRLS","JIRAL","JI랄","JJTV","JOT",
  "JOUNGYANG","JUKEON","JUNGYANG","KGIRLMOLCA","KGIRLS","KINGSEX","KMOVIE","KOREA포르노","KRTV","KSEX","KSEX.NET","LESBIEN","LIVE10TV","LIVE69TV","LUX",
  "MANABOZI","MARKETING","MASTER","MASTURBATION","MOLCA","MOLCAAV","MOLCACD","MOLCANARA","MOLCAPARTY","MOLCAPORNOTV","MOLCASEX","MOLCASEXNIPON","MOLCATV","MOLCA섹스","MOLCA섹스코리아",
  "MOLCA테이프","MOLCA포르노","MOLCA포르노소라가이드","MRCONDOM","MULYOSEX","NALLARI","NIGIMI","NOC","NOMO","NOODE","NOPANTS","NO예","NUDCOUPLE","NUDE","NUDLNUDE",
  "N조E","O6O","ONECLICK","ONECLLCKPAY","ORAL","ORALSEXTV","P1MANG","PENIS","PENNIS","PENTHOUSE","PETISH","PHONESEC","PHONE색","PHONSEC","PHONSEK",
  "PJ","PLAYBOG","PLAYBOY","PLAYBOZI","PLAZA","PLEYBOG","PLEYBOY","PNNANG","PONDO","PONS","PONSEC","PONSEK","PONZ","PORN","PORNER",
  "PORNO","PORNOBOZI","PORNOMANA","PORNOPLAYBOY","PORNO바다","PORNO애니","PORNSEX","PRNO","QMANG","REALXX","ROOT","RUNSEX","R바","S2X","SECMA",
  "SEKMA","SEX123","SEX18","SEX1818","SEX4969","SEXBBS","SEXBOARD","SEXBUIN","SEXCOREA","SEXEROS","SEXGOODS","SEXHANGAME","SEXJAPAN","SEXJJ","SEXKOREA",
  "SEXKOREA,NET","SEXKOREA.COM","SEXKOREA.NET","SEXKOREA21","SEXMAXX","SEXMOLCA","SEXMOLKA","SEXMOVIE","SEXMUSA","SEXNIPON","SEXPDS","SEXPIA","SEXPORNO","SEXSEXY","SEXSHOP",
  "SEXTV","SEXWAL","SEXWALL.COM","SEXWAREZ","SEXXMOLCATV","SEXY","SEXYADONG","SEXYCLIK","SEXYJAPAN","SEXYMAP","SEXYNMALL","SEXYSOUL","SEXYX","SEXY화상채팅","SEX로리타",
  "SE엑스","SIBAL","SIPAL","SIVAL","SM","SORA'SGUIDE","SORAGUIDE","SORASEX","SORASGUIDE","SORASGUIDE.COM","SOSSEX","SPANKING","SRILANKASEX","SSIBAL","SSIPAL",
  "SSIVAL","SUCK","SUICIDE","SUNGINSITE","SWAP","SWAPING","SYLIVASAINT","SYSOP","Suck","SⓔX","S파트너","S하E자X","S하고E싶다X","TACHY","TEMA",
  "TEME","THEME","TVBOZI","TV플레이보이","TWISTKIM","TWISTKIM.COM","USENET","UUCP","VIVA포르노","VULVA","WEBMASTER","WEBXXX","WONJOSEX","WORLDSEX","XXX비디오",
  "XXX포르노","XX할","X대가리","X동","X등급","X물","X섹X스X해X","X섹스","X위","X지","X털","YADONG","YADONGCLUB","YADONGMOLCA","YAHANBOZI",
  "YAHANNOM","YASA","YASINE","YA설","YESMOLCA","YESSEX","YOUNGSEX","ZASAL","ZAZI","ZOT","ZOTTE","suck","⒮⒠⒳","ⓑⓞⓩⓘ","ⓒⓐ",
  "ⓔ반","ⓞ6ⓞ","ⓞ⑥ⓞ","ⓡⓞⓞⓜ","ⓢEX","はいた","ストキング","ㄱ ㅐ ㅅ ㅐ ㄲ ㅣ","ㄱH뇬","ㄱRㅇR슴","ㄱR슴","ㄱㅐㄱㅏ","ㄱㅐㄱㅣㄴㅑ","ㄱㅐㅅㅐㄲㅣ","ㄱㅐㅅㅐ끼",
  "ㄱㅐㅅㅑ","ㄱㅐ가","ㄱㅐ넘","ㄱㅐ년","ㄱㅐ놈","ㄱㅐ눔","ㄱㅐ늠","ㄱㅐ새ㄲㅣ","ㄱㅐ새끼","ㄱㅐ조옷","ㄱㅐ지랄","ㄱㅣ집애","ㄴㅏ뿌운","ㄴㅏ뿌은","ㄴㅏ뿐",
  "ㄴㅏ쁜","ㄴㅔ즈","ㄴㅕ","ㄴㅣㅇㅐㅁㅣ","ㄴㅣㅇㅣㅁㅣ","ㄷㄷㅣ발","ㄷㅏㅂ6","ㄷㅐ추ㄹ","ㄷㅐ출","ㄷㅓㄴ","ㄷㅣㅈㅓ","ㄷㅣㅈㅕ","ㄷㅣ저","ㄷㅣ져","ㄷㅣ진",
  "ㄷㅣ질","ㄸㅏㄹ중","ㄸㅣ발","ㄸㅣ부랄","ㄸㅣ부럴","ㄹ1","ㄹㅔ즈","ㅁL친","ㅁㅜㄹㅛ","ㅁㅣㅊㅣ넘","ㅁㅣ첸년","ㅁㅣ쳤","ㅁㅣ치넘","ㅁㅣ친","ㅁㅣ친넘",
  "ㅂ ㅏ ㅂ ㅗ","ㅂㄱ술","ㅂㅂㅏ구ㄹㅣ","ㅂㅂㅏ구리","ㅂㅓ","ㅂㅓ즤","ㅂㅕㄴ","ㅂㅕㄴㅌㅐ","ㅂㅕ언","ㅂㅕ잉신","ㅃR","ㅃㅏㄱㅜ","ㅃㅏㄹㅏ","ㅃㅏ구ㄹㅣ","ㅃㅏ구리",
  "ㅃㅏ순","ㅅㅅㅣ벨년","ㅅㅅㅣ부랄","ㅅㅅㅣ불얼","ㅅㅏㄱㄱㅏㅅㅣ","ㅅㅏㄱㄱㅏ시","ㅅㅏㄲㅏ","ㅅㅏㄲㅏㅅㅣ","ㅅㅏ까","ㅅㅏ까ㅅㅣ","ㅅㅏ까시","ㅅㅐㄲㅑ","ㅅㅐㄲㅣ","ㅅㅐㄲㅣㅇㅑ","ㅅㅐ꺄",
  "ㅅㅐ뀌","ㅅㅐ끼","ㅅㅔㄱ","ㅅㅔㄲㅣ","ㅅㅔ끼","ㅅㅔ퀴","ㅅㅣ달년","ㅅㅣ박","ㅅㅣ벨","ㅅㅣ빨","ㅅㅣ입팔","ㅅㅣ팍","ㅅㅣ팔","ㅆ1","ㅆ1발",
  "ㅆ1팔","ㅆI","ㅆL팔","ㅆㅂㄹㅁ","ㅆㅂㄻ","ㅆㅏㄲㅏ","ㅆㅔㄱ","ㅆㅖㄲㅣ","ㅆㅖ끼","ㅆㅣ발","ㅆㅣ벨년","ㅆㅣ부랄","ㅆㅣ불년","ㅆㅣ불얼","ㅆㅣ블년",
  "ㅆ엘","ㅇ ㅐ자","ㅇㅏㄱㅏㄹㅣ","ㅇㅏㄱㅏ리","ㅇㅏ가리","ㅇㅐㅁㅣ","ㅇㅐㅂㅣ","ㅇㅐㅈㅏ","ㅇㅐ년","ㅇㅐ미","ㅇㅐ비","ㅇㅐ자","ㅇㅑ설","ㅇㅑ한사진","ㅇㅔㅁㅣ",
  "ㅇㅔ미","ㅈ1랄","ㅈL랄","ㅈㄱ","ㅈㄱㅈㄱ","ㅈㄱ만남","ㅈㅏ★우","ㅈㅏㅇㅟ","ㅈㅏㅈㅣ","ㅈㅏ우ㅣ","ㅈㅣ랄","ㅉㅈ","ㅉㅣㅉㅣ","ㅉㅣ찌","ㅋㅏㅣ",
  "ㅋㅐM","ㅋㅐㅁ","ㅋㅑㅁ","ㅋㅔ","ㅎㅡㅇㅂㅜㄴ","ㅑ동","㉥ㅓ㉧ㅕ","㉥㉥ㅏ","㉨ㅏ㉨ㅣ","㉮숨","㉻㉶","一進","一進會","亡가","凸",
  "品","夜덩","夜동","夜사","夜설","夜한","昌","晶","望가","野덩","野동","野사","野설","가랑이","가스미",
  "가아슴","갈켜줄녀","갈켜줄여","같이자자","개ㅅㅐㄲㅣ","개가턴","개가툰","개간","개같은","개걸레","개깐","개꼴","개너마","개년??","개노마",
  "개뇨나","개뇬","개뇸","개뇽","개눔","개느마","개늠","개도라이","개때꺄","개때끼","개또라이","개라슥","개보지","개부달","개부랄",
  "개부럴","개부알","개불알","개뼈다구","개뼈다귀","개뼉다구","개뼉따구","개새ㄲㅣ","개새꺄","개새끼?","개새리","개새야","개새이","개새킹","개색",
  "개색기?","개색꺄","개색히","개샛끼","개샛키","개샛킹","개샠","개샤끼","개샤킥","개샥","개세이","개섹","개섺","개셃","개셋키",
  "개셐","개솩","개쇄","개쇄끼","개쇅","개쇅갸","개쇅기","개쇅꺄","개쇅끼","개쇅캬","개쇅키","개쇗","개쇗기","개쇠리","개쇸",
  "개수작","개쉐","개쉐리","개쉐이","개쉐키","개쉑","개쉑갸","개쉑기","개쉑꺄","개쉑끼","개쉑이","개쉑캬","개쉑키","개쉑히","개쉢",
  "개쉨","개쉬리","개쉽","개스끼","개스키","개싀기","개싀밸","개싀킈","개싀키","개싏","개싘","개시키","개십팔","개싯기","개싯끼",
  "개싯키","개싴","개쌍넘","개쌍년","개쌍놈","개쌍눔","개쌍늠","개쌍연","개쌍영","개쌔","개쌔꺄","개쌕","개쌕끼","개썅넘","개썅년",
  "개썅놈","개썅늠","개썅연","개쎅","개쐐리","개쒜","개쒜끼","개쒜리","개쒝","개쒝갸","개쒝기","개쒝꺄","개쒝끼","개쒝캬","개쒝키",
  "개쒯","개쒸","개쒹기","개쓰래기","개씨끼","개씨발","개씨발넘","개씨발놈","개씨발뇸","개씨팕","개씹창","개씹팔","개아이","개염창","개욘",
  "개자싁","개자지","개잡년","개잡놈","개잡뇬","개젓","개젓가튼넘","개젖","개졋","개졎","개조또","개조옷","개족","개좝","개찌질이",
  "개호로년","개호로새끼","개후랄","개후레","개후장","갸숨","갸슴","걔잡넘","걔잡년","걔잡뇬","거시기","걸래가튼뇬","걸래같은년","걸래넘","걸래년",
  "걸래놈","걸레가튼넘","걸레가튼뇸","걸레같은","걸레같은년","걸레같은놈","겁탈","게같은","게너마","게년","게노마","게놈","게뇨나","게뇬","게뇸",
  "게뇽","게눔","게띠발넘","게보지","게부랄","게부알","게새끼","게새리","게색기","게색끼","게샛키","게세꺄","게세끼","게섹","게섹놈아",
  "게셋키","게쉐이","게스끼","게스키","게시끼","게시퀴","게싯끼","게쌔기","게쌕끼?","게쓰래기","게쓰레기","게씨발넘","게자시","게자지","게잡넘",
  "게잡년","게잡뇬","게젓","게젓가튼넘","게좆","게지랄","게호로년","게호로놈","게호로새끼","계뇬","계뇽","계약동거","계약애인","계약앤","계집",
  "계집년","계집애","고공섹스","고아년","고아새끼","고우섹스","고추","고추짱","고패티쉬","고픈녀","고픈여","골갑","공육공","공짜버그","공짜캐쉬",
  "공짜캐시","과부촌","광녀","광년","광뇨늬","광뇨니","광뇬","교환부부","구녕","구멍에쑤실까","국제성인마트","굴래머","굴램","굴레머","굿섹스클럽",
  "그거하고파","그거할남","그거할녀","그거할년","그거할사람","그거할여","그넘","그년","그놈","그룹섹","그룹섹스","그지새끼","그지새키","그지좃밥","그지좃빱",
  "그지좆밥","글래머샵","기엉아","기집","기집년","기집애","기집에","기형아","까대","까발려","까발리","까진년","깜둥이","깝쳐","깝쵸",
  "깝치냐","깝치네","깝친","깝친다","깝칠래","깨디얼려","깨디올려","깨띠얼려","깨띠올려","깨쉑","깨쉬얼려","깨쉬올려","깨시얼려","깨쌔꺄","깨쌔끼",
  "깸비","꺼벙이","꺼어저","꺼어저라","꺼어져","꺼어져라","꺼져??","꼬라바","꼬추","꼬츄","꼴갑","꼴갚","꼴려","꼴리는","꼴캅",
  "꼽냐","꽁까","끄져","끄지라","나너먹을께","나를싸게","나를흥분","나먹어줘","나뿌운","나쁘은","나쁜년","나쁜뇬","나아뿐","나아쁀","나아쁜",
  "나체","남녀섹시속옷","남녀자위기구","남성단련용품","남성자위기구","내미랄","내미럴","넣게벌려","넣고싸고","네버섹스","네에미","넴병","년놈","년아","년의자식",
  "노노무라","노답","노답충","노란국물","노랑국물","노브라","노콘","노팬츠","노팬티","농아","뇌절","뇌절충","뇬","누두","누드몰카",
  "누드뮤직","누드사진","누드섹스","누드쇼","누드스케치","누드집","누드천사","누드커플","누드클럽","누드필름","누드화보","눈깔","뉘기미","뉘미","뉘미럴",
  "뉘미롤","뉘이미","뉘잉기미","뉭기미","느검마","느그애미","느그애비","늬귀미","늬기미","늬믜창","늬미","니ㅇㅐ미","니귀미","니기미","니나",
  "니넘","니넴","니년","니놈","니뮈","니믜","니미??","니미롤","니미를","니보지","니새끼","니앰","니어미","니에미","니에비",
  "니엠","니이미","니좆","니죠때","니죶데","니주가리","님이랄","닝기미","다리벌려","다리벌리","다보자성인영화관","다음섹스","다이섹스","닥치셔","단란주점",
  "답없","답없는","대가빡","대갈통","대박성인토탈몰","대주까","대줄년","대포통장","대포폰","더러운년","더티섹스","던주꾸마","덥치기","덥침","덩신",
  "덮치기","덮침","데져라","데진다","데질래","도그플","도란년","도른것","도른넘","도른년","도른뇬","도른뇸","도우미","도우미800","도움주는사람",
  "도쿄섹스","도쿄섹스NIPON","돈버는","돈선불","돌림빵","돌은것","돌은년","돌은놈","돌은뇬","돌은뇸","돌흔","돔만남","돔알지","돔줘요","동침",
  "돼지년","돼지놈","돼지새끼","돼진다","되가리","되질래","됻밥","두두섹스","둥신","뒈지","뒈진다","뒤길넘","뒤길놈","뒤로너어줘","뒤즬",
  "뒤진다","듀ㄱㅓ","듀거","듀글래","드러운년","드응신","등급보류성인영화관","등띤","듸랄","듸뢀","듸즬","듸진다","딍싄","딍신","디ㅈㅕ",
  "디랄","디럴","디뢀","디봘","디저","디져","디져라","디지털섹스조선","디진다","디질","디질래","딥닥ㅊㅕ","딥콜","딩구","딩쉰",
  "딩신","따먹기","따먹는","따먹혀줄래","따무라","따묵기","따이고시퍼","딱지떼자","딱쵸","딱치셔","딸따뤼","딸따리","딸치는","때꺄","떠라이",
  "떡걸","떡촌","떡치기","떨아이","떵개","떵싸구","또ㄹㅏㅇㅣ","또ㄹㅏ이","똘라이","똘른놈","똘뱅이","똘봉년","똘아이","똥개","똥고",
  "똥구멍","똥꾸녕","똥닦다","똥대가리","똥물","똥쳐먹다","뚱뚱한","뚱보","뛰발","뛰봘","뜁새","뜨건밤","뜨발","뜨벌","띄랄",
  "띄발","띄밸","띄뱅","띄벨","띕알","띠랄","띠바","띠박","띠발","띠방","띠밸","띠벌","띠벨","띠부랄","띠부럴",
  "띠불","띠붕","띠뷀","띠브랄","띠브럴","띠블","띠이바","띠이발","띠이방","띠이밸","띠이벌","띠이불","띠이붕","띠이팔","띠이펄",
  "띠이풀","띠팔","띠펄","띠풀","띱때","띱뽕","띱뿡","띱새","띱세","띱쎄","라노비아섹스샵","라이브섹스","라이브스트립","러브박사성인용품","러브베드",
  "러브섹시클럽","러브하자성인용품","러브호텔","러시아걸","레드섹스TV","레아뒤","레즈","로리타","롤리타","룸사롱","룸살롱","룸섹스","룸쌀롱","리얼섹","리얼섹스플레이",
  "리얼에로","립서비스","링기미","마니아섹스","마스터베이션","마앙가","마이세이담당","마이세이운영","막간년","막간놈","막간뇬","막간뇸","만져볼래","만져주께","만져줄년",
  "만져줄분","만져줘","만지고파","만지는중","만지면커진다","만화보지","맛간년","맛간놈","맛간뇬","맛간뇸","망가짱","망나니","맞간년","매드피아","매춘",
  "매친","매틘","맬섭","머니올려요","머니헌터","머라빠너","머라빠노","멍텅","메둥","메롱","메친","메틘","멜돔","모노섹스","몰래보기",
  "몰래카메라","몰래캠코더","몸뚱이줄께","몸안에사정","몸켐","몸파는","몸파라","못생긴","뫙가","무료망가","무료몰카","무료성인","무료성인동영상","무료성인만화","무료성인방송",
  "무료성인싸이트","무료성인엽기","무료성인영화","무료성인정보","무료섹스","무료섹스동영상","무료섹스사이트","무료야설","무료포르노","무료포르노동영상","무삭제원판","무전망가","묻지마","물확뺄","뭐칀",
  "뭐퀼래","뭐클래","뭬친놈","뮈칀","뮈칀세끼","뮈칀세리","뮈칀섹끼","뮈칀쉐이","뮈친","뮈친세리","뮈친섹끼","뮈친섹이","뮈친쉑이","믜칀","믜칑",
  "믜친","믜칭","믜칭넘","믜튄","믜틘","믜틴","미국포르노","미륀","미륀놈","미린넘","미린년","미소녀섹스가이드","미스누드","미스터콘돔","미쎄스터",
  "미씨촌","미진놈","미진뇬","미찬놈","미첫","미첸년","미쳤","미춘","미췐","미췬","미췬년","미췬세끼","미췬쉐리","미췽","미츤",
  "미칀","미칑","미치넘","미치년","미치뇬","미친넘","미친뇬","미친세리","미친섹이","미친쉐이","미친쉑이","미친자식","미칧","미튼","미틘",
  "미틩","미틴뇬","미혼모","민셩야설","민증위조","및친","밑구녕","밑구녕빨기","밑창","밑친","바구리","바보탱이","바부탱이","바이브레이터","박고빼고",
  "박고싶다","박고싶퍼","박아줄게","박아줄께","박을께","박을년","박큐","밖에다쌀께","발ㅂㅏㅂㅓ","발ㅂㅏ부","발ㅂㅏ브","발넘","발년","발바버","발바부",
  "발바브","밟ㅇㅏㅂㅓ","밟아버","밟아부","밟아브","밤일","밥팅","배위에싸죠","배째","백보디","밸년","밸놈","뱅쉰","뱅싄","뱅시",
  "뱅신","뱅틴","뱡신","버그프로그램","버러지","버얼려서너어","버즤","버지물","버짓물","버케","벅쿠","벅큐","번개남","번색","번섹",
  "번쌕","번쎅","벌기","벌렁거려","벌레","벌레같은","벌레새끼","벌려","벌릴여자","벙개남","벙섹","벙신","벙어리","벨년","벨놈",
  "벵쉰","벵싄","벵틴","벼ㄴ","벼언","벼잉신","변GIR","변GIRL","변T","변YOU녀","변ㄴㅕ","변강쇠","변강쐬","변견","변녀",
  "변섹","병닥","병딘","병딲","병띤","병삼","병순","병쉔","병쉰","병싀","병신??","병신쉐리","병신쉐이","병싡","병싢",
  "병싱","병씮","병엉신","병원놀이","병자","병탁","보Z","보ㅈㅣ","보오지","보주ㅣ","보지걸","보지구녕","보지구멍","보지나라","보지녀",
  "보지년","보지뇬","보지당","보지물","보지벌","보지보지","보지털","보짓","보짓년","보짓물","보짖","보짖물","보찌","보털","본디지",
  "봉신","봉알","뵝신","뵤즤","뵤지","뵨태","뵹딱","뵹싄","부랄","부럴","부부섹스","부운영","부카케","부킹맨","불륜",
  "불알","붐붐고수","붕딱","붕딲","붕뛴","붕싄","붕시나","붕신","붕알","붜지","붱싄","뷍슨","뷍싄","뷍신","뷩쉬",
  "뷩쉰","뷩싄","뷩신","뷰웅신","븅딱","븅띤","븅싄","븅시","븅신","븅아","븅틴","브라자","브라쟈","브랄","브레지어",
  "블알","븡딱","븡쉰","븡신","븽딱","븽쉬","븽슨","븽싄","븽시","븽신","비됴방","비러머","비러먹","비밀번호를넣으세요","비실명",
  "비용신","비웅신","비잉신","빌어먹","빙ㅅㅣ","빙구","빙닥","빙딱","빙뛴","빙띤","빙순","빙쉬","빙쉰","빙싀","빙싄",
  "빙시","빙신","빙신쉐리","빙신쉐이","빙아","빙탁","빙틴","빠걸","빠구ㄹㅣ","빠구리","빠굴","빠굴이","빠꾸","빠꾸리","빠도리",
  "빠돌이","빠라","빠라줘","빠러","빠르너","빠수니","빠순이","빠큐","빡촌","빡큐","빤스","빤쮸","빤쮸리","빨간마후라","빨게해도",
  "빨고시퍼","빨고시포","빨고싶다","빨고싶어","빨고파","빨구시퍼","빨구싶나","빨구싶어","빨기","빨라줘","빨아","빨아도","빨아봐","빨아봐요","빨아죠",
  "빨아주고싶어","빨아줄게","빨아줄까","빨아줄까요","빨아줄께","빨아줄꼐","빨아줄래","빨아줄수있어","빨아줄여","빨아줘","빨어","빨어핥어박어","빨자","빨자좃","빨통",
  "빽보디","빽보비","빽보지","빽자지","빽큐","뺑신","뺑쒼","뺑씬","뺘아뻐그리","뺘큐","뻐꾸","뻐르나","뻐르너","뻐르노","뻐어큐",
  "뻐큐","뻐킹","뻑가","뻑구","뻑뀨","뻑유","뻑큐","뻘노","뻨규","뻨큐","뽀로노","뽀르나","뽀르너","뽀뽀뽀","뽀쥐",
  "뽀쮜","뽀큐","뽁큐","뽈노","뽈로노","뽕알","뿅신","뿅씬","뿡알","쀼웅신","쁑신","삐구","삐꾸","삐리넷","삑구",
  "삑꾸","삥쉰","삥쒼","삥씬","사ㄱㄱㅏㅅㅣ","사ㄱㄱㅏ시","사ㄲㅏ","사ㄲㅏㅅㅣ","사ㄲㅏ시","사가리","사가지","사갈탱이","사까ㅅㅣ","사까리","사까치",
  "사딸라","사발","사발통문","사앙뉸","사이버섹스","사창가","사카시","사카치","살인현장","삼룡이","삽입","삿깟시","상넘","상년","상노무",
  "상놈","상뇬","새X","새ㄲㅑ","새꺄","새뀌","새끠","새뤼","새에끼","색ㄲㅣ","색ㅋㅑ","색갸","색골","색광","색꺄",
  "색남","색녀","색마","색수","색쉬","색스코리아","색쑤","색쓰","색캬","색파트너","색할","색햐","색히","샊꺄","샙세",
  "생양아치","샠","샤럽","샤불년","샤앙녀","샤앙년","샵년","샹넘","샹년","샹논","샹놈","샹뇬","샹늠","샹악년","샹악연",
  "서양뽀르노","서양이쁜이","성감대","성게시판","성겨육","성경험","성고민상담","성과섹스","성관계","성교제","성기구","성보조기구","성섹스","성숙녀","성욕구",
  "성인CD","성인IJ","성인갤러리","성인게시판","성인게임","성인공간","성인그리고섹스","성인극장","성인나라","성인놀이문화","성인누드","성인뉴스","성인대화","성인대화방","성인동영상",
  "성인드라마","성인만화","성인만화나라","성인만화천국","성인망가","성인몰","성인무료","성인무료동영상","성인무료사이트","성인무료영화","성인무비","성인물","성인미스랭크","성인미팅방","성인방",
  "성인방송","성인방송국","성인방송안내","성인별곡","성인비됴","성인비디오","성인사이트","성인사이트소개","성인사진","성인상품","성인생방송","성인샵","성인서적","성인성교육스쿨","성인소녀경",
  "성인소라가이드","성인소설","성인쇼","성인쇼핑","성인쇼핑몰","성인시트콤","성인싸이트","성인애니","성인애니메이션","성인야동","성인야사","성인야설","성인야캠","성인야화","성인에로무비",
  "성인에로영화","성인엽기","성인엽기DAMOIM","성인영상","성인영화","성인영화관","성인영화나라","성인영화방","성인영화세상","성인영화천국","성인와레즈","성인용","성인용CD","성인용품","성인용품도매센터",
  "성인용품샵","성인용품에로존","성인용품전문쇼핑몰","성인용품할인매장","성인유머","성인이미지","성인일본","성인자료","성인자료실","성인잡지","성인전용관","성인전용정보","성인정보","성인채팅","성인챗",
  "성인천국","성인체위","성인카툰","성인카페","성인컨텐츠","성인클럽","성인포탈","성인플래쉬","성인플래시","성인화상","성인화상채팅","성일플래쉬","성체위","성체험","성충동",
  "성클리닉","성테크닉","성행위","세게빨아","세꺄","세뀌","세끠","세븐누드닷컴","세븐섹시","세액스","세에끼","세에쓰","세에엑스","세엑","세엑수우",
  "세퀴","섹S","섹걸","섹걸닷컴","섹골","섹골닷컴","섹남","섹녀","섹도우즈","섹뜨","섹마","섹무비","섹보지","섹소리","섹수",
  "섹쉬","섹시","섹시TV","섹시WAVE","섹시갤러리","섹시걸","섹시게이트","섹시나라","섹시나이트","섹시누드","섹시뉴스","섹시매거진","섹시맵","섹시무비","섹시사진",
  "섹시샵","섹시성인용품","섹시섹스코리아","섹시스타","섹시신문","섹시씨엔엔","섹시아이제이","섹시에로닷컴","섹시엔TV","섹시엔몰","섹시연예인","섹시재팬","섹시조선","섹시존","섹시짱",
  "섹시촌","섹시코디","섹시코리아","섹시클럽","섹시클릭","섹시팅하자","섹시팬티","섹쑤","섹쓰","섹티쉬","섹티즌","섹파트너","섹하고","섹하구","섹하자",
  "섹하장","섹한번","섹할","섹해","섹히","센스디스","셁기","쇄기","쇄끼","쇅끼","쇅수","쇅스","쇅쑤","쇅쓰","쇍끼",
  "쇗키","쇼걸클럽","쇼우망가","쇼킹","쇼킹동영상","쇼킹섹스","쇼킹섹스코리아","쇼킹에로","수타킹","수퍼맨","숙모보지","숩삭키","쉐꺄","쉐리","쉐이",
  "쉐이빙","쉐키","쉑갸","쉑꺄","쉑수","쉑스","쉑쑤","쉑쓰","쉑이들","쉑캬","쉑히","쉡세","쉣더뻑","쉨기","쉨끼",
  "쉨캬","쉨키","쉬바","쉬바라미","쉬바러미","쉬방","쉬방쉐이","쉬밸","쉬뱅","쉬벌","쉬벧","쉬벨","쉬볼","쉬봘","쉬불",
  "쉬붕","쉬빌","쉬빠??","쉬빡","쉬빨","쉬빵세이","쉬이바","쉬이발","쉬이방","쉬이밸","쉬이벌","쉬이불","쉬이붕","쉬이빡","쉬이빨",
  "쉬이팔","쉬이펄","쉬이풀","쉬파","쉬팍","쉬팔","쉬팕","쉬팛","쉬팡","쉬펄","쉬풀","쉭빨","쉭뻘","쉭셰리","쉭팔",
  "쉻알","쉽ㅅㅐ","쉽ㅅㅔ","쉽새","쉽생구리","쉽샤퀴","쉽세","쉽셋","쉽쌔","쉽쎄","쉽쎅","쉽알","쉽앨","쉽탱","쉽할이",
  "쉿새","쉿세","스너프","스발","스벨","스쿨걸","스킨쉽","스타씹기","스트립쇼","스트립쑈","스패킹","스팽","스팽킹","스펄","습팔",
  "싀방","싀밸","싀벌","싀벨","싀봉","싀빨","싀팍","싀팔","싁팍","싑년","싑샤퀴","싑세","싑알","싑앨","싑얄",
  "싑질","싓팔","시글년","시끼","시달년","시댕","시댕아","시댕이","시뎅","시뎅아","시땡이","시망","시바라미","시박","시발넘",
  "시발새끼","시밝","시밠","시밣","시방새","시방세","시방쉐","시밮","시밯","시밸","시뱅","시뱔","시뱜","시벨넘","시벨새키",
  "시벵","시봘","시봟","시부리","시붕","시브랄","시블","시빌","시아눔","시앙넘","시앙년","시앙놈","시앙눔","시양넘","시양년",
  "시양놈","시양눔","시이방","시이밸","시이벌","시이불","시이붕","시이빡","시이팔","시이펄","시이풀","시입팔","시팍","시팍새꺄","시팍새끼",
  "시팏","시팔넘","시팔년","시팔놈","시팔새꺄","시팕","시팗","시팤","시펑","시퐉","시퐐","식팍","식팔","신마담","실리콘하우스성인용품",
  "실밸","실비아","싫팛늠","심야TV","심한포르노","십녀","십때꺄","십때끼","십떼끼","십발새끼","십발쌔꺄","십밸","십새꺄","십새캬","십새키",
  "십색","십색꺄","십색끼","십색히","십세","십세끼","십세리","십세이","십셰리","십쇄리","십쇡기","십쉐","십쉐리","십쉐이","십쉑",
  "십쉑히","십시끼","십쌔","십쌔꺄","십쎄끼","십씨키","십앋","십알","십왈","십좌지","십지랄","십지럴","십질","십탱","십탱구리",
  "십탱굴이","십탱꾸리","십팔년","십팔련","십팔새끼","십팔연","십할년","십할련","싯끼","싯빨","싯팔","싯퐐","싱발","싲팔","싴팔",
  "싵팔","싶알","싶팔","싸가리","싸가지","싸갈","싸게해줄께","싸고시포","싸달","싸발","싸발년","싸뱔","싸앙넘","싸앙녀","싸앙년",
  "싸앙눔","싸앙뉸","싸앙늠","싸이커","싸줄께","싹년","싹수","싹쓰리닷컴","싹아쥐","싹아지","쌈치기","쌉년","쌍넌","쌍넘","쌍노무",
  "쌍놈아","쌍뇨나","쌍뇬","쌍뇸","쌍뇽","쌍뉸","쌍악연","쌍연","쌔까","쌔꺄","쌔리","쌔밸년","쌔캬","쌤통","쌩MOLCA",
  "쌩몰카","쌩보지","쌩보지쑈","쌩쑈","쌩아다","쌩포르노","쌰갈탱","쌰럽","쌰앙넘","쌰앙녀","쌰앙년","쌰앙눔","쌰앙뉸","쌰앙늠","썁색",
  "썁슥","썁식","썌꺄","써글","써글넘","써글년","써글놈","써글눔","써글늠","써벌","썩을넘","썩을년","썩을놈","썩을눔","썩을늠",
  "쎄꺄","쎄끈","쎄끈한","쎄럽","쎄엑","쎄엑스","쎅","쎼ㄲㅣ","쎽스","쐉넘","쐉년","쐑갸","쐑꺄","쐑수","쐑스",
  "쐑쑤","쐑쓰","쐑캬","쑤벌","쑵년","쑵새","쒜끼","쒜럽","쒜리","쒝기","쒝끼","쒝수","쒝스","쒝쑤","쒝쓰",
  "쒝캬","쒭새","쒸댕아","쒸바","쒸바라","쒸박","쒸방","쒸방쉐이","쒸밸","쒸벌","쒸벨","쒸부","쒸부럴","쒸불","쒸붕",
  "쒸빠","쒸빡","쒸빨","쒸빨넘","쒸빵","쒸빵세이","쒸뻘","쒸이바","쒸이발","쒸이방","쒸이밸","쒸이벌","쒸이부","쒸이불","쒸이붕",
  "쒸이빡","쒸이빨","쒸이팔","쒸이펄","쒸이풀","쒸입새","쒸파","쒸팍","쒸팔","쒸펄","쒸펠","쒸폴","쒸풀","쒸풍","쓉년",
  "쓉발","쓉새","쓉쎄끼","쓉알","쓉질","쓉창","쓉탱","쓋빨","쓋뻘","쓋파라","쓋팍","쓰글","쓰글넘","쓰글년","쓰글놈",
  "쓰글늠","쓰댕","쓰델","쓰뎅","쓰바","쓰바새끼","쓰바세끼","쓰바쌔끼","쓰박","쓰발","쓰발넘","쓰발새끼","쓰발세꺄","쓰발세끼","쓰발쌔꺄",
  "쓰밝","쓰방","쓰밸","쓰버","쓰벌","쓰볼","쓰봉","쓰봘","쓰부","쓰불","쓰붕","쓰뷀","쓰브럴","쓰블","쓰빡",
  "쓰뺄","쓰으방","쓰으벌","쓰이바","쓰이발","쓰이방","쓰이밸","쓰이벌","쓰이부","쓰이불","쓰이붕","쓰이빡","쓰이팔","쓰이펄","쓰이풀",
  "쓰파","쓰팍","쓰팔","쓰펄","쓰풀","쓸모없는","씁새","씁새끼","씁알","씌댈","씌댕","씌델","씌뎅","씌바","씌밝",
  "씌방","씌밸","씌뱅","씌뱌","씌벌","씌벨","씌벵","씌봉","씌봘","씌뵹","씌부려","씌블","씌팍","씌팏","씌팔",
  "씌팕","씌퐁","씦알","씨8","씨꽈랄","씨끼","씨누드21","씨댕","씨댕년","씨댕이","씨델","씨뎅","씨땡이","씨레기","씨뤠기",
  "씨망","씨바라미","씨박","씨박새끼","씨발넘","씨발놈아","씨발뇬","씨발뇸","씨밝","씨밟","씨밠","씨밣","씨밦","씨방새","씨방세",
  "씨방쉐","씨밮","씨밯","씨밸","씨밸년?","씨밸놈","씨뱅","씨뱅가리","씨뱅놈","씨뱅아","씨뱔","씨벌년","씨벧","씨벨년","씨벵",
  "씨봉알","씨봘","씨봙","씨봟","씨봪","씨뵐","씨부랠","씨부럴","씨부렁","씨부려","씨부리","씨부얼","씨불년","씨불알","씨불얼",
  "씨붕","씨뷩","씨브","씨브랄","씨브럴","씨블","씨블년","씨빠","씨빡","씨빯","씨뽀랄","씨뽤","씨뽥","씨뿔","씨쁠",
  "씨앙넘","씨앙년","씨앙놈","씨앙눔","씨양넘","씨양년","씨양념","씨양놈","씨양눔","씨이댕","씨이바","씨이방","씨이밸","씨이벌","씨이부",
  "씨이불","씨이붕","씨이빡","씨이빨","씨이팔","씨이펄","씨이풀","씨입년","씨입뇬","씨탱","씨텡","씨파??","씨팍","씨팍새꺄","씨팍새끼",
  "씨팏","씨팔넘","씨팔년","씨팔뇬","씨팔뇸","씨팕","씨팗","씨팞","씨팡","씨풀","씨풍","씨필","씨할","씰알","씳박",
  "씳발","씳알","씹갓길","씹것","씹넘","씹녀","씹뇬","씹뇸","씹딱꿍","씹때꺄","씹때끼","씹떼","씹떼끼","씹밸","씹벌",
  "씹보지","씹보지년","씹부랄","씹블","씹빡","씹빨","씹빵구","씹뻘","씹뽀지","씹새꺄","씹새리","씹새이","씹새캬","씹새키","씹색꺄",
  "씹색끼","씹색히","씹샛길","씹생알","씹세","씹세끼","씹세이","씹셋나루","씹쉐","씹쉐리","씹쉐이","씹쉑","씹쉑히","씹시끼","씹쌔",
  "씹쌔기","씹쌔꺄","씹쌔끼","씹쌔키","씹쎄","씹쎄끼","씹씨키","씹알","씹연","씹자지년","씹지랄","씹지럴","씹질","씹창년","씹탱",
  "씹탱구리","씹탱꾸리","씹팔년","씹팔련","씹팔쌔끼","씹팔연","씹펄","씹풀","씹할련","씹할연","씻끼","씻발","씻벌","씻뻘","씻퐁",
  "씾팔","앂년","앂팔","아ㄱㅏ리","아가ㄹㅣ","아가리","아갈","아갈이","아갈통","아괄","아구지","아구창","아구통","아굴","아굴창",
  "아마걸포르노","아이러브에로스쿨","아퍼살살해","안에사정","알몸","알몸공개","알몸사진","알몸쇼","알바걸","알바구함","알바녀","알바맨","알바비","알바쪽지","애ㅂㅣ",
  "애ㅈㅏ","애널입4","애년","애로물","애로영화","애마부인","애무","애믜","애미","애미 없는","애미??","애미뒤진","애미없는","애미죽은","애븨",
  "애비","애비 없는","애비??","애비없는","애자","애자매","애쟈","애좌","앰병","앰창","앵벌이","야ㅎㅏㄴ","야게임즈닷넷","야근병동","야께임",
  "야껨","야녀","야덩","야동게시판","야똥","야설","야설공작소","야설신화","야설의문","야섹","야쉬쉬","야스","야시25TV","야시MOLCA","야시MTV",
  "야시걸","야시꾸리","야시녀","야시랭크","야시룸","야시시","야시캠","야시코리아","야애니","야오위","야요이","야자료","야한거","야한걸","야한것",
  "야한게임","야한그림","야한놈","야한놈SEX","야한누드","야한동영상","야한만화","야한밤TV","야한밤티브이","야한사딘","야한사이트","야한사진","야한소설","야한쇼닷컴","야한영화",
  "야한이야기","야한클럽","얀동영상","얀예기","얌ㅁㅏ","얌마","양ㅇㅏㅊㅣ","양ㅇㅏ치","양년","양아","양아치","어리버리","어벙","얼간이","엄마젓",
  "엄창","엉덩이","에니탑","에로","에로69","에로69TV","에로79","에로걸즈","에로게이트","에로게임","에로관","에로극장","에로니폰","에로닷컴","에로당",
  "에로데이","에로동","에로동영상","에로라이브","에로무비","에로물","에로뮤직비디오","에로바다","에로방티브","에로배우","에로비","에로비디오","에로비안나이트","에로샵","에로세일",
  "에로섹스TV","에로소라가이드","에로쇼","에로스","에로스TV","에로스데이","에로스아시아","에로스재팬","에로스코리아","에로스쿨","에로스타","에로스타일","에로스페셜","에로스포유","에로시네마",
  "에로시안닷컴","에로시티","에로씨네","에로아시아","에로야","에로에스","에로엔조이","에로영화","에로영화관","에로올","에로와이프","에로이브","에로존","에로주","에로촬영현장",
  "에로카","에로클릭","에로투유","에로틱코리아","에로파크","에로패티시","에로팬티","에로플래쉬","에로필름","에믜","에미","에미뒤진","에미젓","에미죽은","에밀",
  "에비","엑스노브라","엑스모텔","엑스터시","엠병","엠창","여성성인용품","여성자위기구","여자먹고","여자몸","여자보지","연병","연븅","연인사이성인샵","염병",
  "염병할","염뵹","엽기에로","엽기적인섹스","엿먹어","엿먹어라","영계","옘병","오까마","오랄","오럴","오럴섹스","오르가슴","오르가즘","오마담",
  "오마이에로","오빠아파","오빠아파닷컴","오빠아퍼","오성인","오예성인영화","올가즘","옷버그","옷벗기기","옷벗기기게임","옹녀","와레즈","왕가스","왕따","왕보지",
  "왕소세지","왕자지","요년","요씨댕","욘병","욘뵹","욘븅","욤뵹","욧먹어","우라질","우랄질","운지","원조알바","원초적","원틀릭페이",
  "월경","유두","유방","유호필름","유흥","육갑","육구자세","육시랄","육시럴","윤간","윤락","윤븅","음담패설","음마성전","음박학염",
  "이거본사람은","이걸본사람은","이녀나","이년","이뇬","이반성인용품몰","이시끼","이자식","인터넷성인방송","일본동영상","일본망가","일본미소녀","일본뽀르노","일본성인만화","일본성인방송",
  "일진회","임ㅁㅏ","임마","임빙할","입닥ㅊㅓ","입닥ㅊㅕ","입사후장","입안사정","입주둥이","자JI","자궁","자발적","자슥아","자아지","자위기구",
  "자위남","자위녀","자위씬","자위용품","자위중","자위코리아","자위하자","자위할래","자위행위","자쥐","자지털","잘빨아","잠지털","잡것","잡년",
  "잡놈","잡뇬","잡뇸","잡밥","잡탱년","잦털","재랄","재수없","재수없는","재팬마나","재팬만화","재팬팬티","저까","저까??","저까는",
  "저까튼","저꽈튼","저꽛튼","저넘","저년","저놈","저뇬","저뇸","저빠지게","저빱","저샤키","저자식","저주글","저즌팬티","적가튼",
  "적꽈튼","적셔줄까","적셔줄께","적셔줄래","전나게","전나구려","전조건","전화방","젇놔","점물","접밥","젓가튼","젓가튼쉐이","젓같내","젓같은",
  "젓까","젓까는","젓나","젓나게","젓마난","젓만이","젓만한","젓밥","젓빠지게","젓은팬티","정력","정력강화용품","정력팬티","정력포탈","정박아",
  "정사씬모음","정사채널","정우성인용품전문점","젖","젖가슴","젖가튼","젖같은","젖고픈","젖까","젖까는","젖꼭지","젖나??","젖나게","젖도","젖마난",
  "젖만한","젖물","젖밥","젖빠지게","젖은팬티","젖탱이","젖통","젙니","제이제이일본성인만화","져꽈튼","조刀ㅏ","조가비","조가틍","조교사","조까",
  "조까는","조까라","조까치","조까튼","조깐다","조깥","조껀만남","조끗","조끗네","조둥아리","조랄","조뢀","조루","조빠","조빠라",
  "조빠지게","조빱","조삐리","조오까","조오또","조옥같네","조옷나","조옷나게","조쟁이","조지냐","조진다","조질래","조카툰","조털","족간네",
  "족같네","족같은?","족딸","족밥","졳","존ㄴㅏ","존나??","존나게","존년","존마나","존마난","존마니","존만","존만이","존만한",
  "존망이","존맹구","졷따","졸라게","졸라맨","졸라야한닷컴","졸망","좀만아","좀물","좁년","좁밥","좁빱","좃가튼년","좃가튼뇬","좃간년",
  "좃까리","좃깟네","좃넘","좃니","좃만아","좃털","좄ㄲㅏ","좄까","종간나","종나","종아리","좆같은놈","좆같은새끼","좆년","좆만아",
  "좆만한년","좆만한놈","좆만한새끼","좆먹어","좆빨아","좆새끼","좆털","좇","죄랄","죠까틍","죠카태","죠카테","죠카툰","죠콰떼","죠콰태",
  "죠콰테","죠콰퉤","죤나","죤니","죤만년","죤만놈","죨라","죳","죳나","죴","죵나","죵늬","죶","죹","주거라",
  "주글년","주길년","주댕이","주민등록번호생성기","주민등록생성기","주우둥이","죽고싶지않다면","죽은년","죽을년","죽을놈","죽인년","죽일꺼","죽일놈","줟","줴섭다",
  "쥐랼","쥐럴","쥐롤","쥐뢀","즈랄","즈럴","즉발","즐길녀","즐길성인","즐길여","즤랄","즤랄떠냐","즤랅","즤랼","즤롤",
  "즤뢀","지달","지댈","지랄무지개","지랄삽차기","지랄쌈치기","지랄육갑","지랄이","지랄하","지랄하는","지랄해","지랗","지랠","지룔","지미럴",
  "지좆","지집년","지집애","질알","질질싸","질할","짐승","짐승같은","짐승새끼","집어쳐","짜샤","짜쌰","짜져","짜쥐","짜지",
  "짬지","짭것","짭년","짭놈","짭뇬","짱나","짱나네","째려","쩌껀","쩝밥","쩤밥","쪼ㄲㅏ","쪼가리","쪼갈","쪼건",
  "쪼까","쪼까튼","쪼껀","쪼다","쪼빠라","쪼빤다","쫀ㄴㅏ","쫀물","쫍밥","쫍빱","쫒같","쭈욱","쭈쭈","찌ㅉㅣ","찌거",
  "찌끄래기","찌랄이","찌지리","찌찌","창녀촌","창년","창뇨","창뇬","창여","채널레드TV","채워줘","챵녀","챵년","챵뇬","처녀막",
  "처머거","처먹어","천재님","첫경험","청부","청부살인","청부폭력","체위동영상","쳐먹","쳐먹어","쳐죽일","초칠","촌년","촌놈","최음제",
  "추ㄴㅕ","출장서비스","치마속","카X","카드","카섹","카섹스","카우치","칼라섹스","캐세끼","캐셋키","컘","커닐링구스","컴S","컴X",
  "컴색","컴샷","컴세엑","컴섹","컴쎅","콈","코리아성인가이드","코리아스트립닷컴","코리아엑스파일","코리아포로노","코리안걸스","코섹스","콘끼그","콘돔닥터","콘필",
  "콩까","콩까자","쾌애앰","쾌앰","쾌엠","쿤닐링구스","쿨에로","퀨","크리토리스","크림걸","큰년","큰놈","큰보지","큰자지","클럽AV스타",
  "클럽에로","큼섹","킬링티비","타부코리아","탱구리","토탈에로","퇴폐","투앤투동거클럽","트렌스","트렘플링","특수콘돔","패니스","패드립","패륜","패륜아",
  "패션앤섹스","패티쉬","패티쉬우먼","패티시","팬티","팬티캔디","팰라치오","팸돔","팸섭","퍼르노","퍽큐","펀색","펀섹","페로몬","페미돔",
  "페티걸","페티쉬","페티쉬러브","페티쉬매거진","페티쉬우먼","페티쉬즘","페티쉬코리아","페티시","페팅","펜섭","펜썹","펜티","펠라티오","펨돔","펨섭",
  "펩시맨","평신","포ㄴ","포경","포노","포로노","포르나","포르노사진","포르노세상","포르노섹스","포르노테이프","포르노테입","포르노테잎","포르느","포르로",
  "포온섹","폰X","폰떽","폰번호","폰색","폰세엑","폰섹스","폰쉑","폰쌕","폰쎅","폰으로","폰팅","풋잡","프리섹스","플래이보이",
  "플레이보이2030","플레이보이소라가이드","플레이보이온라인","플레이보조개","플레이보지","피임기구","피임용품","하두리","하드코어","한번꽂자","한번주께","한번줄래","한스테이성인","할타","할터",
  "핥아주께","핥어","핥을래","핧아","핧아줄께","함대주까","함대줄래","함빨자","핫도그TV","핫성인용품","항문","해피에로","핸타이","향기콘돔","허뎝",
  "허럯","허벌","허벌창","허접","허접새끼","허접쌔끼","허좁","허쩝","허쩝새끼","허쩝쌔끼","헤라러브샵","헤로인","헨타이","헬로우콘돔","헴타이",
  "호냥년","호로","호로년","호로새꺄","호로새끼","호로새리","호로색끼","호로쌔꺄","호로자슥","호로자식","호롸","호모섹기","호모쎄끼","호봉세씨","호봉쎄끼",
  "호봉쒜리","호빠","호스트바","호스트빠","호스티스","호양년","호좁","호테스바","호테스빠","혼음","혼탕","홀닥","홀오자식","화끈남","화끈녀",
  "화끈물개","화냥같은","화류","화양년","후까시","후다년","후라질","후래아","후래자식","후레","후레자식","후리자","후릴","후릴년","후장입사",
  "훔쳐보기","흥분시켜","흥분제","흥분할","흥분할녀","히로뽕","不건전","Ｏ６Ｏ",
];


function filterProfanity(text) {
  // Normalize: remove spaces/dots/special chars between Korean chars for detection
  const normalize = (s) => {
    return s
      .replace(/[\s.·•_\-~=+*#@!,;:'"()\[\]{}|/\\]+/g, "") // strip separators
      .replace(/[0oO]/g, m => m) // keep for leet detection
      .toLowerCase();
  };

  const normalized = normalize(text);
  let result = text;

  // 0. Check phonetic variants (direct match)
  for (const word of PHONETIC_VARIANTS) {
    if (word.length < 1) continue;
    try {
      const regex = new RegExp(word.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi");
      result = result.replace(regex, m => "♡".repeat(m.length));
    } catch {}
  }

  // 0.5 Phonetic normalization check
  const phoneticNorm = (s) => {
    let r = s;
    for (const [from, to] of Object.entries(PHONETIC_MAP)) {
      r = r.split(from).join(to);
    }
    return r;
  };
  const pNorm = phoneticNorm(normalized);
  for (const word of BASE_PROFANITY) {
    const nw = phoneticNorm(normalize(word));
    if (nw.length < 2) continue;
    if (pNorm.includes(nw) && !BASE_PROFANITY.some(w => result.includes("♡".repeat(w.length)))) {
      try {
        const regex = new RegExp(word.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi");
        result = result.replace(regex, m => "♡".repeat(m.length));
      } catch {}
    }
  }

  // 1. Check base words (direct match in normalized form)
  for (const word of BASE_PROFANITY) {
    const nw = normalize(word);
    if (nw.length < 1) continue;
    // Check in normalized text
    if (normalized.includes(nw)) {
      // Find and replace in original text (fuzzy position)
      try {
        const regex = new RegExp(
          word.split("").map(c => c.replace(/[.*+?^${}()|[\]\\]/g, "\\$&") + "[\s.·•_\-~=+*]*").join(""),
          "gi"
        );
        result = result.replace(regex, m => "♡".repeat(Math.max(m.replace(/\s/g,"").length, 1)));
      } catch {}
    }
    // Also try direct regex on original
    try {
      const directRegex = new RegExp(word.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi");
      result = result.replace(directRegex, m => "♡".repeat(m.length));
    } catch {}
  }

  // 2. Check consonant-only patterns
  for (const group of CHOSUNG_MAP) {
    for (const pattern of group) {
      if (text.includes(pattern) || normalized.includes(pattern.replace(/\s/g,""))) {
        const regex = new RegExp(pattern.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "g");
        result = result.replace(regex, m => "♡".repeat(m.length));
        // Also without spaces
        const noSpace = pattern.replace(/\s/g, "");
        if (noSpace !== pattern) {
          const regex2 = new RegExp(noSpace.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "g");
          result = result.replace(regex2, m => "♡".repeat(m.length));
        }
      }
    }
  }

  // 3. Check leet/substitution patterns
  for (const group of LEET_PATTERNS) {
    for (const pattern of group) {
      const regex = new RegExp(pattern.replace(/[.*+?^${}()|[\]\\]/g, "\\$&"), "gi");
      result = result.replace(regex, m => "♡".repeat(m.length));
    }
  }

  // 4. Detect spaced-out profanity (e.g., "시 발", "병 신", "씨 발")  
  const spacedPatterns = [
    [/시\s*발/g], [/씨\s*발/g], [/씨\s*빨/g], [/시\s*팔/g], [/씨\s*팔/g],
    [/병\s*신/g], [/지\s*랄/g], [/개\s*새\s*끼/g], [/새\s*끼/g],
    [/닥\s*쳐/g], [/꺼\s*져/g], [/미\s*친/g], [/느\s*금\s*마/g],
    [/존\s*나/g], [/졸\s*라/g], [/좆\s*같/g], [/좆\s*까/g],
    [/따\s*먹/g], [/떡\s*치/g], [/보\s*지/g], [/자\s*지/g],
    [/f\s*u\s*c\s*k/gi], [/s\s*h\s*i\s*t/gi], [/b\s*i\s*t\s*c\s*h/gi],
    [/a\s*s\s*s/gi], [/d\s*i\s*c\s*k/gi], [/p\s*u\s*s\s*s\s*y/gi],
  ];
  for (const [regex] of spacedPatterns) {
    result = result.replace(regex, m => "♡".repeat(m.replace(/\s/g,"").length));
  }

  return result;
}

// ============================================================
// FEATURE CONSTANTS
// ============================================================

// Auto-replace keyword map (키워드 → 표시명 자동 변환)
// 새 항목 추가는 여기에 { from: "키워드", to: "변환결과" } 추가하면 됨
const AUTO_REPLACE = [
  { from: "샤갈", to: "Marc Chagall" },
];

function applyAutoReplace(text) {
  if (!text) return text;
  let result = text;
  for (const { from, to } of AUTO_REPLACE) {
    result = result.split(from).join(to);
  }
  return result;
}

const QUICK_EMOJIS = ["👍","😂","🤔","💡","👏","❤️","🔥","😮"];

const PRAISE_STICKERS = [
  { emoji: "🌟", label: "풀이 깔끔!" },
  { emoji: "🧠", label: "설명 천재!" },
  { emoji: "💪", label: "끈기 대장!" },
  { emoji: "🚀", label: "속도 최고!" },
  { emoji: "🎯", label: "정답 적중!" },
  { emoji: "🤝", label: "친절한 도우미!" },
];

const AVATAR_ICONS = [
  "🐱","🐶","🐰","🦊","🐻","🐼","🐨","🦁","🐯","🐸",
  "🦉","🐧","🐬","🦄","🐲","🌸","⭐","🌙","☁️","🍀",
];

const PLAZA_THEMES = [
  { id: "default", name: "기본", bg: null, emoji: "🏛️" },
  { id: "exam", name: "시험 기간", bg: "linear-gradient(180deg, #2d1b1b08 0%, #ff634720 100%)", emoji: "🔥" },
  { id: "beach", name: "방학", bg: "linear-gradient(180deg, #e0f7fa15 0%, #fff9c420 100%)", emoji: "🏖️" },
  { id: "cherry", name: "벚꽃", bg: "linear-gradient(180deg, #fce4ec18 0%, #f8bbd020 100%)", emoji: "🌸" },
  { id: "night", name: "야간 자습", bg: "linear-gradient(180deg, #1a237e10 0%, #311b9220 100%)", emoji: "🌙" },
  { id: "snow", name: "겨울", bg: "linear-gradient(180deg, #e3f2fd15 0%, #bbdefb20 100%)", emoji: "❄️" },
  { id: "party", name: "파티", bg: "linear-gradient(135deg, #f8bbd018 0%, #ce93d820 50%, #80deea18 100%)", emoji: "🎉" },
];

const TEACHER_STATUSES = [
  { id: "online", label: "접속 중", emoji: "🟢", color: "#4CAF50" },
  { id: "teaching", label: "수업 중", emoji: "📚", color: "#FF9800" },
  { id: "answering", label: "질문 받는 중", emoji: "✋", color: "#2196F3" },
  { id: "away", label: "자리비움", emoji: "🚶", color: "#9E9E9E" },
  { id: "busy", label: "바쁨", emoji: "⛔", color: "#f44336" },
];

// Simple LaTeX-like renderer: $x^2$ → x², $\frac{a}{b}$ → a/b
function renderMathText(text) {
  if (!text) return text;
  const parts = text.split(/(\$[^$]+\$)/g);
  return parts.map((part, i) => {
    if (part.startsWith("$") && part.endsWith("$")) {
      let math = part.slice(1, -1);
      // Superscript: x^2 → x²
      math = math.replace(/\^{([^}]+)}/g, (_, exp) => {
        const superMap = { "0":"⁰","1":"¹","2":"²","3":"³","4":"⁴","5":"⁵","6":"⁶","7":"⁷","8":"⁸","9":"⁹",
          "n":"ⁿ","i":"ⁱ","+":"⁺","-":"⁻","(":"⁽",")":"⁾","x":"ˣ" };
        return exp.split("").map(c => superMap[c] || c).join("");
      });
      math = math.replace(/\^(.)/g, (_, c) => {
        const superMap = { "0":"⁰","1":"¹","2":"²","3":"³","4":"⁴","5":"⁵","6":"⁶","7":"⁷","8":"⁸","9":"⁹","n":"ⁿ" };
        return superMap[c] || c;
      });
      // Subscript: x_1 → x₁
      math = math.replace(/_(.)/g, (_, c) => {
        const subMap = { "0":"₀","1":"₁","2":"₂","3":"₃","4":"₄","5":"₅","6":"₆","7":"₇","8":"₈","9":"₉" };
        return subMap[c] || c;
      });
      // Fraction: \frac{a}{b} → a/b
      math = math.replace(/\\frac{([^}]+)}{([^}]+)}/g, "($1/$2)");
      // Greek: \alpha → α etc
      const greek = { "\\alpha":"α","\\beta":"β","\\gamma":"γ","\\delta":"δ","\\pi":"π","\\theta":"θ","\\sigma":"σ","\\omega":"ω","\\sqrt":"√","\\pm":"±","\\times":"×","\\div":"÷","\\leq":"≤","\\geq":"≥","\\neq":"≠","\\infty":"∞","\\angle":"∠","\\triangle":"△","\\perp":"⊥","\\parallel":"∥" };
      for (const [k, v] of Object.entries(greek)) {
        math = math.split(k).join(v);
      }
      return <span key={i} style={{ fontFamily: "'Cambria Math', 'Times New Roman', serif", fontStyle: "italic", color: "#6a5acd", fontSize: "0.95em" }}>{math}</span>;
    }
    return part;
  });
}

// ============================================================
// MAIN RENDER FUNCTION
// ============================================================

export function renderPlazaScreen(ctx) {
  const { theme, user, userRole, members, setScreen, showMsg, playSfx, hasPerm,
    chatMsg, setChatMsg, chatLog, setChatLog, chatEndRef, chatNotif, setChatNotif,
    plazaCalls, callUser, ROLES, themeKey } = ctx;

  // ---- Freeze state ----
  const isFrozen = (() => {
    try { return localStorage.getItem("ar_chat_frozen") === "true"; } catch { return false; }
  })();
  if (!window._frozenSynced) {
    window._frozenSynced = true;
    fbGet("settings").then(d => {
      if (d && d.chatFrozen !== undefined) {
        const remote = d.chatFrozen === true || d.chatFrozen === "true";
        localStorage.setItem("ar_chat_frozen", remote ? "true" : "false");
        if (remote !== isFrozen) setChatLog(prev => [...prev]);
      }
    });
  }

  const toggleFreeze = () => {
    const next = !isFrozen;
    localStorage.setItem("ar_chat_frozen", next ? "true" : "false");
    fbSet("settings", { chatFrozen: next });
    showMsg(next ? "🧊 광장이 얼었습니다" : "🔥 광장이 녹았습니다", 2000);
    playSfx(next ? "click" : "success");
    setChatLog(prev => [...prev]);
  };

  // ---- Profanity warning system (#17) ----
  const getWarnings = () => { try { return JSON.parse(localStorage.getItem("ar_warn") || "{}"); } catch { return {}; } };
  const addWarning = (userName) => {
    const warns = getWarnings();
    warns[userName] = (warns[userName] || 0) + 1;
    localStorage.setItem("ar_warn", JSON.stringify(warns));
    fbSet("plaza", { warnings: warns });
    if (warns[userName] >= 3) {
      const mutes = getMutes();
      mutes[userName] = Date.now() + 10 * 60 * 1000;
      localStorage.setItem("ar_mute", JSON.stringify(mutes));
      fbSet("plaza", { mutes });
      showMsg(`⚠️ ${userName} 경고 3회 누적 → 10분 뮤트`, 3000);
    } else {
      showMsg(`⚠️ ${userName} 경고 ${warns[userName]}/3`, 2000);
    }
  };
  const getMutes = () => { try { return JSON.parse(localStorage.getItem("ar_mute") || "{}"); } catch { return {}; } };
  const isMuted = (userName) => {
    const mutes = getMutes();
    return mutes[userName] && mutes[userName] > Date.now();
  };

  // ---- Send chat (enhanced with profanity warning) ----
  const sendChat = (extraFields = {}) => {
    if (isFrozen && userRole !== "admin") {
      showMsg("🧊 광장이 얼어있어요!", 1500); return;
    }
    const myName = user?.nickname || user?.name || "익명";
    if (isMuted(myName)) {
      showMsg("🔇 뮤트 상태입니다. 잠시 후 시도하세요.", 2000); return;
    }
    if (!chatMsg.trim() && !extraFields.type) return;
    const original = chatMsg.trim();
    const filtered = original ? filterProfanity(original) : "";
    const hadProfanity = filtered !== original;
    const cleanText = applyAutoReplace(filtered);

    const newMsg = {
      user: myName, role: userRole,
      text: cleanText, time: Date.now(),
      reactions: {}, replyTo: null,
      ...extraFields,
    };
    const updated = [...chatLog, newMsg].slice(-200);
    setChatLog(updated);
    localStorage.setItem("ar_chat", JSON.stringify(updated));
    fbSet("plaza", { chat: updated });
    setChatMsg("");
    setTimeout(() => chatEndRef.current?.scrollIntoView({ behavior: "smooth" }), 100);

    if (hadProfanity) addWarning(myName);
  };

  // ---- Time filtering ----
  const now = Date.now();
  // filteredLog: data-level — pinned messages persist so 공지 영역에 계속 남음
  const filteredLog = chatLog.filter(m => {
    if (m.pinned) return true; // Pinned messages don't expire (data-level)
    return now - m.time < 10 * 60 * 1000;
  });
  if (filteredLog.length !== chatLog.length) {
    setChatLog(filteredLog);
    localStorage.setItem("ar_chat", JSON.stringify(filteredLog));
  }
  // visibleLog: chat-area only — pinned messages also disappear from the stream after 10 min
  // (but they stay in pinnedMsgs / 공지 영역 forever until unpinned)
  const visibleLog = filteredLog.filter(m => now - m.time < 10 * 60 * 1000);

  const myName = user?.nickname || user?.name || "익명";

  // ---- Online users ----
  const onlineUsers = (() => {
    try {
      const online = JSON.parse(localStorage.getItem("ar_online") || "{}");
      return Object.entries(online).filter(([, v]) => Date.now() - v.time < 30000).map(([name, v]) => ({ name, role: v.role, avatar: v.avatar, status: v.status }));
    } catch { return []; }
  })();
  if (user && !window._presenceSynced) {
    window._presenceSynced = true;
    const myAvatar = (() => { try { return localStorage.getItem("ar_avatar_" + myName) || "🐱"; } catch { return "🐱"; } })();
    const myStatus = (() => { try { return localStorage.getItem("ar_status_" + myName) || ""; } catch { return ""; } })();
    const teacherSt = (() => { try { return localStorage.getItem("ar_teacher_status") || "online"; } catch { return "online"; } })();
    fbSet("plaza", { [`online_${myName}`]: { time: Date.now(), role: userRole, avatar: myAvatar, status: myStatus, teacherStatus: userRole === "admin" ? teacherSt : undefined } });
  }

  const deleteChat = (time) => {
    const updated = chatLog.filter(m => m.time !== time);
    setChatLog(updated);
    localStorage.setItem("ar_chat", JSON.stringify(updated));
    fbSet("plaza", { chat: updated });
  };

  const teacherMember = members.find(m => m.role === "admin");
  const teacherName = teacherMember ? (teacherMember.nickname || teacherMember.name) : "선생님";
  const teacherOnline = onlineUsers.some(u => u.role === "admin");
  const teacherStatus = (() => {
    const t = onlineUsers.find(u => u.role === "admin");
    if (!t) return null;
    try { return localStorage.getItem("ar_teacher_status") || "online"; } catch { return "online"; }
  })();
  const recentCalls = plazaCalls.filter(c => now - c.time < 10 * 60 * 1000).slice(-10);
  const roleColors = { admin: PASTEL.coral, assistant: PASTEL.lavender, student: PASTEL.sky, external: PASTEL.sage };

  // ---- Pinned messages (#3) ----
  const pinnedMsgs = filteredLog.filter(m => m.pinned);
  const togglePin = (time) => {
    const updated = chatLog.map(m => m.time === time ? { ...m, pinned: !m.pinned } : m);
    setChatLog(updated);
    localStorage.setItem("ar_chat", JSON.stringify(updated));
    fbSet("plaza", { chat: updated });
    showMsg("📌 고정 상태 변경됨", 1500);
  };

  // ---- Reactions (#1) ----
  const addReaction = (msgTime, emoji) => {
    const updated = chatLog.map(m => {
      if (m.time === msgTime) {
        const reactions = { ...(m.reactions || {}) };
        const key = emoji;
        if (!reactions[key]) reactions[key] = [];
        if (reactions[key].includes(myName)) {
          reactions[key] = reactions[key].filter(n => n !== myName);
          if (reactions[key].length === 0) delete reactions[key];
        } else {
          reactions[key] = [...reactions[key], myName];
        }
        return { ...m, reactions };
      }
      return m;
    });
    setChatLog(updated);
    localStorage.setItem("ar_chat", JSON.stringify(updated));
    fbSet("plaza", { chat: updated });
  };

  // ---- Reply (#6) ----
  const replyTarget = (() => { try { return JSON.parse(localStorage.getItem("ar_reply") || "null"); } catch { return null; } })();
  const setReplyTarget = (msg) => {
    localStorage.setItem("ar_reply", msg ? JSON.stringify({ user: msg.user, text: msg.text?.slice(0, 40), time: msg.time }) : "null");
    setChatLog(prev => [...prev]); // trigger re-render
  };
  const sendChatWithReply = () => {
    const reply = replyTarget;
    sendChat(reply ? { replyTo: reply } : {});
    setReplyTarget(null);
  };

  // ---- Avatar & Status (#11) ----
  const myAvatar = (() => { try { return localStorage.getItem("ar_avatar_" + myName) || "🐱"; } catch { return "🐱"; } })();
  const myStatusMsg = (() => { try { return localStorage.getItem("ar_status_" + myName) || ""; } catch { return ""; } })();

  // ---- Panel states (using localStorage to persist across re-renders) ----
  const getPanel = () => { try { return localStorage.getItem("ar_plaza_panel") || ""; } catch { return ""; } };
  const setPanel = (p) => { localStorage.setItem("ar_plaza_panel", p); setChatLog(prev => [...prev]); };
  const activePanel = getPanel();

  // ---- Plaza theme (#14) ----
  const plazaThemeId = (() => { try { return localStorage.getItem("ar_plaza_theme") || "default"; } catch { return "default"; } })();
  const plazaTheme = PLAZA_THEMES.find(t => t.id === plazaThemeId) || PLAZA_THEMES[0];

  // ---- Shared timer (#22) ----
  const getTimer = () => { try { return JSON.parse(localStorage.getItem("ar_timer") || "null"); } catch { return null; } };
  const timer = getTimer();
  const timerRemaining = timer ? Math.max(0, timer.end - now) : 0;

  // ---- Daily challenge (#9) ----
  const getChallenge = () => { try { return JSON.parse(localStorage.getItem("ar_challenge") || "null"); } catch { return null; } };
  const challenge = getChallenge();

  // ---- Ranking (#23) ----
  const getRanking = () => {
    const stats = {};
    chatLog.forEach(m => { stats[m.user] = (stats[m.user] || 0) + 1; });
    return Object.entries(stats).sort((a, b) => b[1] - a[1]).slice(0, 5);
  };

  // ---- Praise (#13) ----
  const getPraises = () => { try { return JSON.parse(localStorage.getItem("ar_praises") || "{}"); } catch { return {}; } };
  const sendPraise = (targetName, sticker) => {
    const praises = getPraises();
    if (!praises[targetName]) praises[targetName] = [];
    praises[targetName].push({ from: myName, sticker, time: Date.now() });
    localStorage.setItem("ar_praises", JSON.stringify(praises));
    fbSet("plaza", { praises });
    showMsg(`${sticker.emoji} ${targetName}에게 "${sticker.label}" 전송!`, 2000);
    playSfx("success");
    setPanel("");
  };

  // ---- Wrong note share (#NEW) ----
  // Lazy-load my wrong notes (cached on window object)
  const loadMyWrongNotes = () => {
    if (window._wrongNotesLoading) return;
    window._wrongNotesLoading = true;
    dbGetAllNotes(user?.id || "anon")
      .then(notes => {
        window._wrongNotesCache = Array.isArray(notes) ? notes : [];
        window._wrongNotesLoading = false;
        setChatLog(prev => [...prev]);
      })
      .catch(e => {
        console.error("[plaza] wrong notes load:", e);
        window._wrongNotesCache = [];
        window._wrongNotesLoading = false;
        setChatLog(prev => [...prev]);
      });
  };
  // Resize a base64 image to a smaller version for transport.
  const resizeBase64 = (dataUrl, maxDim, quality) => new Promise((resolve) => {
    const img = new Image();
    img.onload = () => {
      let w = img.width, h = img.height;
      if (w > maxDim || h > maxDim) {
        const s = maxDim / Math.max(w, h);
        w = Math.round(w * s);
        h = Math.round(h * s);
      }
      const c = document.createElement("canvas");
      c.width = w; c.height = h;
      c.getContext("2d").drawImage(img, 0, 0, w, h);
      try {
        resolve({ dataUrl: c.toDataURL("image/jpeg", quality), width: w, height: h });
      } catch {
        resolve({ dataUrl, width: img.width, height: img.height });
      }
    };
    img.onerror = () => resolve({ dataUrl, width: 400, height: 300 });
    img.src = dataUrl;
  });
  // Send a wrong-note as a chat card (with comment)
  const sendWrongNote = async (note, comment) => {
    if (!note?.photoBase64) { showMsg("이미지가 없는 노트입니다", 1500); return; }
    if (isFrozen && userRole !== "admin") { showMsg("🧊 광장이 얼어있어요!", 1500); return; }
    if (isMuted(myName)) { showMsg("🔇 뮤트 상태입니다", 1500); return; }
    try {
      // Aggressive resize: 400px max, JPEG 0.6 → ~30-60KB, safe for Firestore
      const { dataUrl, width, height } = await resizeBase64(note.photoBase64, 400, 0.6);
      const newMsg = {
        user: myName, role: userRole,
        type: "wrong_note",
        photoBase64: dataUrl,
        photoW: width, photoH: height,
        text: (comment || "").trim(),
        noteId: note.id,
        time: Date.now(), reactions: {},
      };
      const updated = [...chatLog, newMsg].slice(-200);
      setChatLog(updated);
      localStorage.setItem("ar_chat", JSON.stringify(updated));
      fbSet("plaza", { chat: updated });
      setPanel("");
      setTimeout(() => chatEndRef.current?.scrollIntoView({ behavior: "smooth" }), 100);
      showMsg("📓 오답노트 공유 완료!", 1500);
      playSfx("success");
    } catch (e) {
      console.error("[plaza] send wrong note:", e);
      showMsg("이미지 처리 실패", 2000);
    }
  };

  // ---- Chat log export (#18) ----
  const exportLog = () => {
    const lines = filteredLog.map(m => {
      const t = new Date(m.time).toLocaleString("ko-KR");
      return `[${t}] ${m.user}: ${m.text}${m.type === "poll" ? " (투표)" : ""}${m.type === "sos" ? " (도와줘!)" : ""}`;
    }).join("\n");
    const blob = new Blob([lines], { type: "text/plain;charset=utf-8" });
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url; a.download = `광장_로그_${new Date().toLocaleDateString("ko-KR")}.txt`;
    a.click();
    URL.revokeObjectURL(url);
    showMsg("📋 로그 저장 완료!", 1500);
  };

  // ---- Whiteboard (#21) ----
  const getWhiteboardData = () => { try { return JSON.parse(localStorage.getItem("ar_wb") || "null"); } catch { return null; } };

  // ---- Poll helpers (#2) ----
  const votePoll = (msgTime, optIndex) => {
    const updated = chatLog.map(m => {
      if (m.time === msgTime && m.type === "poll") {
        const votes = { ...(m.votes || {}) };
        // Remove previous vote
        Object.keys(votes).forEach(k => {
          if (votes[k]?.includes(myName)) {
            votes[k] = votes[k].filter(n => n !== myName);
          }
        });
        if (!votes[optIndex]) votes[optIndex] = [];
        votes[optIndex].push(myName);
        return { ...m, votes };
      }
      return m;
    });
    setChatLog(updated);
    localStorage.setItem("ar_chat", JSON.stringify(updated));
    fbSet("plaza", { chat: updated });
  };

  // ---- Message renderer helper ----
  const renderMsgContent = (msg) => {
    // Poll message (#2)
    if (msg.type === "poll") {
      const totalVotes = Object.values(msg.votes || {}).reduce((s, arr) => s + (arr?.length || 0), 0);
      return (
        <div>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 6, color: PASTEL.lavender }}>📊 {msg.question}</div>
          {msg.options?.map((opt, i) => {
            const voteCount = (msg.votes?.[i] || []).length;
            const pct = totalVotes > 0 ? Math.round(voteCount / totalVotes * 100) : 0;
            const voted = (msg.votes?.[i] || []).includes(myName);
            return (
              <button key={i} onClick={() => votePoll(msg.time, i)} style={{
                display: "block", width: "100%", marginBottom: 4, padding: "6px 10px",
                borderRadius: 8, border: voted ? `2px solid ${PASTEL.coral}` : `1px solid ${theme.border}`,
                background: `linear-gradient(90deg, ${PASTEL.coral}${voted ? "30" : "12"} ${pct}%, transparent ${pct}%)`,
                color: theme.text, fontSize: 11, textAlign: "left", cursor: "pointer",
                transition: "all 0.2s",
              }}>
                {opt} <span style={{ float: "right", color: theme.textSec, fontSize: 10 }}>{voteCount}표 ({pct}%)</span>
              </button>
            );
          })}
          <div style={{ fontSize: 9, color: theme.textSec, marginTop: 4 }}>총 {totalVotes}명 참여</div>
        </div>
      );
    }
    // SOS message (#8)
    if (msg.type === "sos") {
      return (
        <div style={{ padding: "6px 0" }}>
          <div style={{ fontSize: 20, textAlign: "center", marginBottom: 4 }}>🆘</div>
          <div style={{ fontSize: 12, fontWeight: 700, color: "#f44336", textAlign: "center" }}>도와주세요!</div>
          {msg.text && <div style={{ fontSize: 11, color: theme.text, marginTop: 4 }}>{renderMathText(msg.text)}</div>}
        </div>
      );
    }
    // Problem card (#7)
    if (msg.type === "problem_card") {
      return (
        <div style={{ padding: "6px 0" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 6, marginBottom: 4 }}>
            <span style={{ fontSize: 16 }}>📝</span>
            <span style={{ fontSize: 11, fontWeight: 700, color: PASTEL.coral }}>문제 공유</span>
          </div>
          <div style={{ fontSize: 12, color: theme.text, padding: "8px 10px", background: `${PASTEL.yellow}20`, borderRadius: 8, border: `1px solid ${PASTEL.yellow}` }}>
            {renderMathText(msg.text)}
          </div>
          {msg.hint && <div style={{ fontSize: 10, color: theme.textSec, marginTop: 4 }}>💡 {msg.hint}</div>}
        </div>
      );
    }
    // Whiteboard image (#21)
    if (msg.type === "whiteboard") {
      return (
        <div>
          <div style={{ fontSize: 11, fontWeight: 700, color: PASTEL.sky, marginBottom: 4 }}>🎨 화이트보드</div>
          {msg.imageData && <img src={msg.imageData} alt="whiteboard" style={{ width: "100%", maxWidth: 250, borderRadius: 8, border: `1px solid ${theme.border}` }} />}
          {msg.text && <div style={{ fontSize: 11, color: theme.text, marginTop: 4 }}>{msg.text}</div>}
        </div>
      );
    }
    // Wrong note share (#NEW)
    if (msg.type === "wrong_note") {
      return (
        <div>
          <div style={{ fontSize: 11, fontWeight: 700, color: PASTEL.coral, marginBottom: 4, display: "flex", alignItems: "center", gap: 4 }}>
            <span>📓</span> 오답노트 공유
          </div>
          {msg.photoBase64 && (
            <img
              src={msg.photoBase64}
              alt="오답노트"
              onClick={() => window.open(msg.photoBase64, "_blank")}
              style={{
                width: "100%", maxWidth: 280, borderRadius: 8,
                border: `1px solid ${theme.border}`, display: "block",
                cursor: "zoom-in", background: "#fff",
              }}
            />
          )}
          {msg.text && (
            <div style={{
              fontSize: 12, color: theme.text, marginTop: 6, padding: "6px 10px",
              background: `${PASTEL.coral}10`, borderRadius: 8,
              borderLeft: `2px solid ${PASTEL.coral}`,
              fontStyle: "italic", lineHeight: 1.4,
            }}>"{msg.text}"</div>
          )}
        </div>
      );
    }
    // Praise notification (#13)
    if (msg.type === "praise") {
      return (
        <div style={{ textAlign: "center", padding: "4px 0" }}>
          <span style={{ fontSize: 24 }}>{msg.stickerEmoji}</span>
          <div style={{ fontSize: 11, color: PASTEL.coral, fontWeight: 700 }}>{msg.from} → {msg.to}</div>
          <div style={{ fontSize: 10, color: theme.textSec }}>{msg.stickerLabel}</div>
        </div>
      );
    }
    // Challenge (#9)
    if (msg.type === "challenge") {
      return (
        <div style={{ padding: "6px 0" }}>
          <div style={{ fontSize: 14, fontWeight: 700, color: PASTEL.coral, textAlign: "center" }}>⚡ 오늘의 도전 문제</div>
          <div style={{ fontSize: 12, color: theme.text, marginTop: 6, padding: "8px 10px", background: `${PASTEL.coral}10`, borderRadius: 8, border: `1px dashed ${PASTEL.coral}` }}>
            {renderMathText(msg.text)}
          </div>
        </div>
      );
    }
    // Normal text with math support (#20)
    return (
      <p style={{ fontSize: 13, color: theme.text, margin: 0, lineHeight: 1.5, fontFamily: "'Noto Serif KR', serif" }}>
        {renderMathText(msg.text)}
      </p>
    );
  };

  // ============================================================
  // RENDER
  // ============================================================
  return (
    <div style={{
      height: "100vh", maxHeight: "100dvh", display: "flex", flexDirection: "column",
      background: plazaTheme.bg || theme.bg, fontFamily: "'Noto Serif KR', serif", position: "relative",
    }}>

      {/* Freeze overlay with snowfall */}
      {isFrozen && (
        <div style={{
          position: "absolute", top: 0, left: 0, right: 0, bottom: 0,
          background: "linear-gradient(180deg, rgba(181,213,232,0.08) 0%, rgba(181,213,232,0.15) 100%)",
          pointerEvents: "none", zIndex: 50,
        }}>
          {[...Array(12)].map((_, i) => (
            <div key={i} style={{
              position: "absolute", left: `${8 + (i * 7.5) % 85}%`, top: `-${10 + (i * 13) % 20}px`,
              fontSize: [10, 8, 12, 9, 11, 7, 10, 8, 13, 9, 11, 8][i],
              opacity: 0.3 + (i % 3) * 0.15,
              animation: `snowfall ${4 + (i % 3) * 2}s linear ${(i * 0.5) % 3}s infinite`,
            }}>
              {["❄", "❅", "❆", "✦"][i % 4]}
            </div>
          ))}
        </div>
      )}

      <style>{`
        @keyframes snowfall {
          0% { transform: translateY(-20px) rotate(0deg); opacity: 0; }
          10% { opacity: 0.5; }
          90% { opacity: 0.3; }
          100% { transform: translateY(100vh) rotate(360deg); opacity: 0; }
        }
        @keyframes freezePulse {
          0%, 100% { box-shadow: 0 0 0 0 rgba(181,213,232,0.3); }
          50% { box-shadow: 0 0 12px 4px rgba(181,213,232,0.15); }
        }
        @keyframes fadeIn { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes pulse { 0%,100% { transform: scale(1); } 50% { transform: scale(1.08); } }
        @keyframes timerPulse { 0%,100% { opacity: 1; } 50% { opacity: 0.6; } }
        .plaza-content { position: relative; }
        @media print { .plaza-content { display: none !important; } }
        .plaza-btn:active { transform: scale(0.95); }
        .reaction-btn:hover { transform: scale(1.2); }
      `}</style>

      {/* ======== HEADER ======== */}
      <div style={{
        flexShrink: 0, display: "flex", alignItems: "center", padding: "12px 16px",
        borderBottom: `1px solid ${isFrozen ? PASTEL.sky : theme.border}`,
        animation: isFrozen ? "freezePulse 3s ease infinite" : "none",
      }}>
        <button onClick={() => { playSfx("click"); setScreen("menu"); }}
          style={{ background: "none", border: "none", color: theme.textSec, fontSize: 13, cursor: "pointer" }}>← 메뉴</button>
        <span style={{ flex: 1, textAlign: "center", fontSize: 14, fontWeight: 700, color: isFrozen ? PASTEL.sky : theme.text, fontFamily: "'Playfair Display', serif" }}>
          {isFrozen ? "🧊 광장" : `${plazaTheme.emoji} 광장`}
        </span>
        <div style={{ display: "flex", alignItems: "center", gap: 6 }}>
          {/* Tools button */}
          <button onClick={() => setPanel(activePanel === "tools" ? "" : "tools")}
            style={{ background: activePanel === "tools" ? `${PASTEL.coral}20` : "none", border: `1px solid ${activePanel === "tools" ? PASTEL.coral : theme.border}`, borderRadius: 8, padding: "3px 7px", fontSize: 11, cursor: "pointer", color: theme.textSec }}>
            ≡
          </button>
          {userRole === "admin" && (
            <button onClick={toggleFreeze} style={{
              background: isFrozen ? `${PASTEL.sky}25` : "none",
              border: `1px solid ${isFrozen ? PASTEL.sky : theme.border}`,
              borderRadius: 8, padding: "3px 8px", fontSize: 11, cursor: "pointer",
              color: isFrozen ? PASTEL.sky : theme.textSec,
            }}>
              {isFrozen ? "🔥" : "🧊"}
            </button>
          )}
          <span style={{ fontSize: 11, color: PASTEL.mint }}>
            <span style={{ display: "inline-block", width: 6, height: 6, borderRadius: 3, background: PASTEL.mint, marginRight: 4 }} />
            {onlineUsers.length}
          </span>
        </div>
      </div>

      {/* ======== TOOLS PANEL ======== */}
      {activePanel === "tools" && (
        <div style={{ flexShrink: 0, padding: "8px 12px", borderBottom: `1px solid ${theme.border}`, background: `${theme.card}ee`, animation: "fadeIn 0.2s ease" }}>
          <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
            {/* Poll button (#2) */}
            <button onClick={() => setPanel("poll")} className="plaza-btn" style={toolBtnStyle(theme)}>📊 투표</button>
            {/* SOS button (#8) */}
            <button onClick={() => {
              sendChat({ type: "sos" });
              setPanel("");
            }} className="plaza-btn" style={toolBtnStyle(theme)}>🆘 도와줘!</button>
            {/* Problem share (#7) */}
            <button onClick={() => setPanel("problem")} className="plaza-btn" style={toolBtnStyle(theme)}>📝 문제공유</button>
            {/* Whiteboard (#21) */}
            <button onClick={() => setPanel("whiteboard")} className="plaza-btn" style={toolBtnStyle(theme)}>🎨 보드</button>
            {/* Wrong note share (#NEW) */}
            <button onClick={() => {
              loadMyWrongNotes();
              setPanel("wrongnote");
            }} className="plaza-btn" style={toolBtnStyle(theme)}>📓 오답노트</button>
            {/* Timer (#22) */}
            {userRole === "admin" && <button onClick={() => setPanel("timer")} className="plaza-btn" style={toolBtnStyle(theme)}>⏱️ 타이머</button>}
            {/* Challenge (#9) */}
            {userRole === "admin" && <button onClick={() => setPanel("challenge")} className="plaza-btn" style={toolBtnStyle(theme)}>⚡ 도전문제</button>}
            {/* Theme (#14) */}
            {userRole === "admin" && <button onClick={() => setPanel("theme")} className="plaza-btn" style={toolBtnStyle(theme)}>🎨 테마</button>}
            {/* Teacher status (#24) */}
            {userRole === "admin" && <button onClick={() => setPanel("teacherStatus")} className="plaza-btn" style={toolBtnStyle(theme)}>📡 상태</button>}
            {/* Profile (#11) */}
            <button onClick={() => setPanel("profile")} className="plaza-btn" style={toolBtnStyle(theme)}>😺 프로필</button>
            {/* Ranking (#23) */}
            <button onClick={() => setPanel("ranking")} className="plaza-btn" style={toolBtnStyle(theme)}>🏆 랭킹</button>
            {/* Export (#18) */}
            {userRole === "admin" && <button onClick={() => { exportLog(); setPanel(""); }} className="plaza-btn" style={toolBtnStyle(theme)}>📋 저장</button>}
          </div>
        </div>
      )}

      {/* ======== SUB-PANELS ======== */}

      {/* Poll creation panel (#2) */}
      {activePanel === "poll" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>📊 투표 만들기</div>
          <input id="poll-q" placeholder="질문" autoComplete="off" style={inputStyle(theme)} />
          <input id="poll-a" placeholder="보기 (쉼표로 구분: A, B, C)" autoComplete="off" style={{ ...inputStyle(theme), marginTop: 6 }} />
          <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
            <button onClick={() => {
              const q = document.getElementById("poll-q")?.value;
              const opts = document.getElementById("poll-a")?.value?.split(",").map(s => s.trim()).filter(Boolean);
              if (!q || !opts?.length || opts.length < 2) { showMsg("질문과 보기 2개 이상 입력!", 1500); return; }
              const newMsg = { user: myName, role: userRole, type: "poll", question: q, options: opts, votes: {}, time: Date.now(), reactions: {} };
              const updated = [...chatLog, newMsg].slice(-200);
              setChatLog(updated);
              localStorage.setItem("ar_chat", JSON.stringify(updated));
              fbSet("plaza", { chat: updated });
              setPanel("");
              setTimeout(() => chatEndRef.current?.scrollIntoView({ behavior: "smooth" }), 100);
            }} style={actionBtnStyle(PASTEL.coral)}>올리기</button>
            <button onClick={() => setPanel("")} style={actionBtnStyle(theme.textSec)}>취소</button>
          </div>
        </div>
      )}

      {/* Problem share panel (#7) */}
      {activePanel === "problem" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>📝 문제 공유</div>
          <input id="prob-text" placeholder="문제 내용 ($x^2$ 수식 가능)" autoComplete="off" style={inputStyle(theme)} />
          <input id="prob-hint" placeholder="힌트 (선택)" autoComplete="off" style={{ ...inputStyle(theme), marginTop: 6 }} />
          <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
            <button onClick={() => {
              const text = document.getElementById("prob-text")?.value;
              if (!text) { showMsg("문제를 입력하세요!", 1500); return; }
              const hint = document.getElementById("prob-hint")?.value || "";
              const newMsg = { user: myName, role: userRole, type: "problem_card", text, hint, time: Date.now(), reactions: {} };
              const updated = [...chatLog, newMsg].slice(-200);
              setChatLog(updated);
              localStorage.setItem("ar_chat", JSON.stringify(updated));
              fbSet("plaza", { chat: updated });
              setPanel("");
              setTimeout(() => chatEndRef.current?.scrollIntoView({ behavior: "smooth" }), 100);
            }} style={actionBtnStyle(PASTEL.coral)}>공유</button>
            <button onClick={() => setPanel("")} style={actionBtnStyle(theme.textSec)}>취소</button>
          </div>
        </div>
      )}

      {/* Timer panel (#22) */}
      {activePanel === "timer" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>⏱️ 공유 타이머</div>
          <div style={{ display: "flex", gap: 6 }}>
            {[1, 3, 5, 10, 15].map(m => (
              <button key={m} onClick={() => {
                const timerData = { end: Date.now() + m * 60000, label: `${m}분 타이머`, started: Date.now() };
                localStorage.setItem("ar_timer", JSON.stringify(timerData));
                fbSet("plaza", { timer: timerData });
                showMsg(`⏱️ ${m}분 타이머 시작!`, 2000);
                setPanel("");
                setChatLog(prev => [...prev]);
              }} style={{ ...actionBtnStyle(PASTEL.coral), padding: "6px 12px" }}>{m}분</button>
            ))}
          </div>
          <button onClick={() => {
            localStorage.removeItem("ar_timer");
            fbSet("plaza", { timer: null });
            setPanel("");
            setChatLog(prev => [...prev]);
          }} style={{ ...actionBtnStyle(theme.textSec), marginTop: 6 }}>타이머 종료</button>
        </div>
      )}

      {/* Challenge panel (#9) */}
      {activePanel === "challenge" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>⚡ 오늘의 도전 문제</div>
          <textarea id="challenge-text" placeholder="도전 문제 입력 ($x^2$ 수식 가능)" rows={3} style={{ ...inputStyle(theme), resize: "vertical" }} />
          <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
            <button onClick={() => {
              const text = document.getElementById("challenge-text")?.value;
              if (!text) { showMsg("문제를 입력하세요!", 1500); return; }
              const newMsg = { user: myName, role: userRole, type: "challenge", text, time: Date.now(), reactions: {}, pinned: true };
              const updated = [...chatLog, newMsg].slice(-200);
              setChatLog(updated);
              localStorage.setItem("ar_chat", JSON.stringify(updated));
              fbSet("plaza", { chat: updated });
              setPanel("");
              setTimeout(() => chatEndRef.current?.scrollIntoView({ behavior: "smooth" }), 100);
            }} style={actionBtnStyle(PASTEL.coral)}>출제</button>
            <button onClick={() => setPanel("")} style={actionBtnStyle(theme.textSec)}>취소</button>
          </div>
        </div>
      )}

      {/* Theme panel (#14) */}
      {activePanel === "theme" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>🎨 광장 테마</div>
          <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
            {PLAZA_THEMES.map(t => (
              <button key={t.id} onClick={() => {
                localStorage.setItem("ar_plaza_theme", t.id);
                fbSet("settings", { plazaTheme: t.id });
                showMsg(`${t.emoji} ${t.name} 테마 적용!`, 1500);
                setPanel("");
              }} style={{
                padding: "6px 12px", borderRadius: 10,
                border: plazaThemeId === t.id ? `2px solid ${PASTEL.coral}` : `1px solid ${theme.border}`,
                background: plazaThemeId === t.id ? `${PASTEL.coral}15` : theme.card,
                fontSize: 11, cursor: "pointer", color: theme.text,
              }}>
                {t.emoji} {t.name}
              </button>
            ))}
          </div>
        </div>
      )}

      {/* Teacher status panel (#24) */}
      {activePanel === "teacherStatus" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>📡 선생님 상태 설정</div>
          <div style={{ display: "flex", gap: 6, flexWrap: "wrap" }}>
            {TEACHER_STATUSES.map(s => {
              const current = (() => { try { return localStorage.getItem("ar_teacher_status") || "online"; } catch { return "online"; } })();
              return (
                <button key={s.id} onClick={() => {
                  localStorage.setItem("ar_teacher_status", s.id);
                  fbSet("plaza", { teacherStatus: s.id });
                  showMsg(`${s.emoji} ${s.label}`, 1500);
                  setPanel("");
                  setChatLog(prev => [...prev]);
                }} style={{
                  padding: "6px 12px", borderRadius: 10,
                  border: current === s.id ? `2px solid ${s.color}` : `1px solid ${theme.border}`,
                  background: current === s.id ? `${s.color}20` : theme.card,
                  fontSize: 11, cursor: "pointer", color: theme.text,
                }}>
                  {s.emoji} {s.label}
                </button>
              );
            })}
          </div>
        </div>
      )}

      {/* Profile panel (#11) */}
      {activePanel === "profile" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>😺 프로필 설정</div>
          <div style={{ fontSize: 11, color: theme.textSec, marginBottom: 6 }}>아바타 선택</div>
          <div style={{ display: "flex", gap: 6, flexWrap: "wrap", marginBottom: 10 }}>
            {AVATAR_ICONS.map(a => (
              <button key={a} onClick={() => {
                localStorage.setItem("ar_avatar_" + myName, a);
                showMsg(`${a} 아바타 변경!`, 1000);
                setChatLog(prev => [...prev]);
              }} style={{
                fontSize: 18, padding: "4px 6px", borderRadius: 8, cursor: "pointer",
                border: myAvatar === a ? `2px solid ${PASTEL.coral}` : `1px solid ${theme.border}`,
                background: myAvatar === a ? `${PASTEL.coral}15` : "transparent",
              }}>{a}</button>
            ))}
          </div>
          <div style={{ fontSize: 11, color: theme.textSec, marginBottom: 4 }}>상태 메시지</div>
          <input id="status-msg" defaultValue={myStatusMsg} placeholder="열공 중 🔥" maxLength={20} autoComplete="off"
            style={inputStyle(theme)} />
          <button onClick={() => {
            const v = document.getElementById("status-msg")?.value || "";
            localStorage.setItem("ar_status_" + myName, v);
            showMsg("상태 메시지 저장!", 1000);
            setPanel("");
          }} style={{ ...actionBtnStyle(PASTEL.coral), marginTop: 6 }}>저장</button>
        </div>
      )}

      {/* Ranking panel (#23) */}
      {activePanel === "ranking" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>🏆 오늘의 랭킹</div>
          {getRanking().length === 0 && <div style={{ fontSize: 11, color: theme.textSec }}>아직 데이터가 없어요</div>}
          {getRanking().map(([name, count], i) => (
            <div key={name} style={{ display: "flex", alignItems: "center", gap: 8, padding: "4px 0" }}>
              <span style={{ fontSize: 14, width: 24, textAlign: "center" }}>{i === 0 ? "🥇" : i === 1 ? "🥈" : i === 2 ? "🥉" : `${i + 1}`}</span>
              <span style={{ fontSize: 12, color: theme.text, flex: 1 }}>{name}</span>
              <span style={{ fontSize: 11, color: PASTEL.coral, fontWeight: 700 }}>{count}회</span>
            </div>
          ))}
          {/* Praise counts */}
          {(() => {
            const praises = getPraises();
            const entries = Object.entries(praises).filter(([, arr]) => arr.length > 0).sort((a, b) => b[1].length - a[1].length).slice(0, 3);
            if (entries.length === 0) return null;
            return (
              <div style={{ marginTop: 10 }}>
                <div style={{ fontSize: 11, fontWeight: 700, color: PASTEL.lavender, marginBottom: 4 }}>🌟 칭찬왕</div>
                {entries.map(([name, arr]) => (
                  <div key={name} style={{ fontSize: 11, color: theme.text, padding: "2px 0" }}>
                    {name}: {arr.length}개 받음
                  </div>
                ))}
              </div>
            );
          })()}
          <button onClick={() => setPanel("")} style={{ ...actionBtnStyle(theme.textSec), marginTop: 8 }}>닫기</button>
        </div>
      )}

      {/* Praise panel (#13) */}
      {activePanel.startsWith("praise:") && (() => {
        const targetName = activePanel.split(":")[1];
        return (
          <div style={subPanelStyle(theme)}>
            <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>🌟 {targetName}에게 칭찬 보내기</div>
            <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}>
              {PRAISE_STICKERS.map(s => (
                <button key={s.label} onClick={() => {
                  sendPraise(targetName, s);
                  // Also post as chat message
                  const newMsg = { user: myName, role: userRole, type: "praise", from: myName, to: targetName, stickerEmoji: s.emoji, stickerLabel: s.label, time: Date.now(), reactions: {} };
                  const updated = [...chatLog, newMsg].slice(-200);
                  setChatLog(updated);
                  localStorage.setItem("ar_chat", JSON.stringify(updated));
                  fbSet("plaza", { chat: updated });
                }} style={{
                  display: "flex", flexDirection: "column", alignItems: "center", gap: 2,
                  padding: "8px 12px", borderRadius: 12, border: `1px solid ${theme.border}`,
                  background: theme.card, cursor: "pointer", fontSize: 11,
                }}>
                  <span style={{ fontSize: 22 }}>{s.emoji}</span>
                  <span style={{ color: theme.textSec, fontSize: 10 }}>{s.label}</span>
                </button>
              ))}
            </div>
            <button onClick={() => setPanel("")} style={{ ...actionBtnStyle(theme.textSec), marginTop: 8 }}>취소</button>
          </div>
        );
      })()}

      {/* Whiteboard panel (#21) */}
      {activePanel === "whiteboard" && (
        <div style={subPanelStyle(theme)}>
          <div style={{ fontSize: 12, fontWeight: 700, marginBottom: 8 }}>🎨 화이트보드</div>
          <canvas id="wb-canvas" width={280} height={180} style={{
            border: `1px solid ${theme.border}`, borderRadius: 8, background: "#fff", cursor: "crosshair", touchAction: "none",
          }}
            onPointerDown={e => {
              const c = document.getElementById("wb-canvas");
              const ctx2 = c?.getContext("2d");
              if (!ctx2) return;
              const rect = c.getBoundingClientRect();
              ctx2._drawing = true;
              ctx2.beginPath();
              ctx2.moveTo(e.clientX - rect.left, e.clientY - rect.top);
              ctx2.strokeStyle = "#333";
              ctx2.lineWidth = 2;
              ctx2.lineCap = "round";
              c.setPointerCapture(e.pointerId);
            }}
            onPointerMove={e => {
              const c = document.getElementById("wb-canvas");
              const ctx2 = c?.getContext("2d");
              if (!ctx2?._drawing) return;
              const rect = c.getBoundingClientRect();
              ctx2.lineTo(e.clientX - rect.left, e.clientY - rect.top);
              ctx2.stroke();
            }}
            onPointerUp={() => {
              const c = document.getElementById("wb-canvas");
              const ctx2 = c?.getContext("2d");
              if (ctx2) ctx2._drawing = false;
            }}
          />
          <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
            <button onClick={() => {
              const c = document.getElementById("wb-canvas");
              if (!c) return;
              const imageData = c.toDataURL("image/png", 0.6);
              const newMsg = { user: myName, role: userRole, type: "whiteboard", imageData, text: "", time: Date.now(), reactions: {} };
              const updated = [...chatLog, newMsg].slice(-200);
              setChatLog(updated);
              localStorage.setItem("ar_chat", JSON.stringify(updated));
              fbSet("plaza", { chat: updated });
              setPanel("");
              setTimeout(() => chatEndRef.current?.scrollIntoView({ behavior: "smooth" }), 100);
            }} style={actionBtnStyle(PASTEL.coral)}>전송</button>
            <button onClick={() => {
              const c = document.getElementById("wb-canvas");
              const ctx2 = c?.getContext("2d");
              if (ctx2) ctx2.clearRect(0, 0, 280, 180);
            }} style={actionBtnStyle(PASTEL.sky)}>지우기</button>
            <button onClick={() => setPanel("")} style={actionBtnStyle(theme.textSec)}>취소</button>
          </div>
        </div>
      )}

      {/* Wrong note share panel (#NEW) */}
      {activePanel === "wrongnote" && (() => {
        const notes = window._wrongNotesCache;
        const loading = window._wrongNotesLoading || notes === undefined;
        const selectedId = (() => { try { return localStorage.getItem("ar_plaza_wn_selected") || ""; } catch { return ""; } })();
        const selectNote = (id) => { localStorage.setItem("ar_plaza_wn_selected", id); setChatLog(prev => [...prev]); };
        const selected = notes?.find(n => n.id === selectedId) || null;
        return (
          <div style={subPanelStyle(theme)}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
              <div style={{ fontSize: 12, fontWeight: 700 }}>📓 오답노트 공유</div>
              <button onClick={() => { loadMyWrongNotes(); }} style={{
                background: "none", border: `1px solid ${theme.border}`, borderRadius: 6,
                padding: "2px 8px", fontSize: 10, color: theme.textSec, cursor: "pointer",
              }}>↻ 새로고침</button>
            </div>

            {loading && (
              <div style={{ textAlign: "center", padding: "20px 0", color: theme.textSec, fontSize: 11 }}>
                불러오는 중...
              </div>
            )}

            {!loading && (!notes || notes.length === 0) && (
              <div style={{ textAlign: "center", padding: "20px 0", color: theme.textSec, fontSize: 11 }}>
                오답노트가 없어요.<br />
                <span style={{ fontSize: 10 }}>오답노트 화면에서 먼저 문제를 추가해보세요!</span>
              </div>
            )}

            {!loading && notes && notes.length > 0 && (
              <>
                <div style={{
                  display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 6,
                  maxHeight: 180, overflowY: "auto", padding: 2,
                }}>
                  {notes.map(n => {
                    const isSel = n.id === selectedId;
                    return (
                      <button key={n.id} onClick={() => selectNote(n.id)} style={{
                        position: "relative", padding: 0, borderRadius: 8,
                        border: isSel ? `2px solid ${PASTEL.coral}` : `1px solid ${theme.border}`,
                        background: theme.card, cursor: "pointer", overflow: "hidden",
                        aspectRatio: "1 / 1",
                      }}>
                        {n.photoBase64 ? (
                          <img src={n.photoBase64} alt="note" style={{
                            width: "100%", height: "100%", objectFit: "cover", display: "block",
                          }} />
                        ) : (
                          <div style={{ fontSize: 20, color: theme.textSec, lineHeight: "60px" }}>📄</div>
                        )}
                        {isSel && (
                          <div style={{
                            position: "absolute", top: 2, right: 2,
                            background: PASTEL.coral, color: "#fff", borderRadius: 10,
                            width: 16, height: 16, fontSize: 10, fontWeight: 700,
                            display: "flex", alignItems: "center", justifyContent: "center",
                          }}>✓</div>
                        )}
                      </button>
                    );
                  })}
                </div>

                {selected && (
                  <div style={{ marginTop: 10, padding: "8px 10px", background: `${PASTEL.coral}08`, borderRadius: 8, border: `1px solid ${PASTEL.coral}30` }}>
                    <div style={{ fontSize: 10, color: theme.textSec, marginBottom: 6 }}>
                      📅 {new Date(selected.createdAt).toLocaleDateString("ko-KR")}
                    </div>
                    <textarea
                      id="wn-comment"
                      placeholder="이 문제에 대해 한마디 (예: 이거 어떻게 풀어요?)"
                      rows={2}
                      maxLength={200}
                      style={{ ...inputStyle(theme), resize: "vertical", fontSize: 12 }}
                    />
                    <div style={{ display: "flex", gap: 6, marginTop: 8 }}>
                      <button onClick={() => {
                        const comment = document.getElementById("wn-comment")?.value || "";
                        sendWrongNote(selected, comment);
                        localStorage.removeItem("ar_plaza_wn_selected");
                      }} style={actionBtnStyle(PASTEL.coral)}>📤 공유</button>
                      <button onClick={() => { localStorage.removeItem("ar_plaza_wn_selected"); setChatLog(prev => [...prev]); }}
                        style={actionBtnStyle(theme.textSec)}>선택 해제</button>
                    </div>
                  </div>
                )}
              </>
            )}

            <button onClick={() => { localStorage.removeItem("ar_plaza_wn_selected"); setPanel(""); }}
              style={{ ...actionBtnStyle(theme.textSec), marginTop: 8 }}>닫기</button>
          </div>
        );
      })()}

      {/* ======== SHARED TIMER BAR (#22) ======== */}
      {timer && timerRemaining > 0 && (
        <div style={{
          flexShrink: 0, padding: "6px 16px", borderBottom: `1px solid ${theme.border}`,
          background: `${PASTEL.coral}12`, display: "flex", alignItems: "center", gap: 8,
          animation: "timerPulse 2s ease infinite",
        }}>
          <span style={{ fontSize: 16 }}>⏱️</span>
          <div style={{ flex: 1 }}>
            <div style={{ fontSize: 11, color: theme.textSec }}>{timer.label || "타이머"}</div>
            <div style={{ fontSize: 16, fontWeight: 700, color: PASTEL.coral, fontFamily: "'Playfair Display', serif" }}>
              {Math.floor(timerRemaining / 60000)}:{String(Math.floor((timerRemaining % 60000) / 1000)).padStart(2, "0")}
            </div>
          </div>
          <div style={{ width: 100, height: 4, borderRadius: 2, background: `${theme.border}` }}>
            <div style={{
              height: 4, borderRadius: 2, background: PASTEL.coral,
              width: `${timer.started ? (1 - timerRemaining / (timer.end - timer.started)) * 100 : 0}%`,
              transition: "width 1s linear",
            }} />
          </div>
        </div>
      )}

      {/* ======== PINNED MESSAGES (#3) ======== */}
      {pinnedMsgs.length > 0 && (
        <div style={{ flexShrink: 0, padding: "6px 16px", borderBottom: `1px solid ${theme.border}`, background: `${PASTEL.yellow}12` }}>
          {pinnedMsgs.slice(-3).map((m, i) => {
            const hasNicknameKeyword = m.text && m.text.includes("닉네임");
            return (
              <div key={i} style={{ padding: "2px 0" }}>
                <div style={{ fontSize: 11, color: theme.text, display: "flex", alignItems: "center", gap: 4 }}>
                  <span>📌</span>
                  <span style={{ fontWeight: 600 }}>{m.user}:</span>
                  <span style={{ flex: 1, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{m.text?.slice(0, 50)}</span>
                  {userRole === "admin" && <button onClick={() => togglePin(m.time)} style={{ background: "none", border: "none", fontSize: 9, color: theme.textSec, cursor: "pointer" }}>✕</button>}
                </div>
                {hasNicknameKeyword && (
                  <div style={{ display: "flex", justifyContent: "flex-start", marginTop: 4, marginLeft: 18 }}>
                    <button
                      onClick={() => {
                        try { sessionStorage.setItem("ar_open_settings_nickname", "1"); } catch {}
                        playSfx("click");
                        setScreen("student-home");
                      }}
                      style={{
                        padding: "4px 12px",
                        borderRadius: 10,
                        border: "none",
                        background: `linear-gradient(135deg, ${PASTEL.coral}, ${PASTEL.dustyRose})`,
                        color: "white",
                        fontSize: 11,
                        fontWeight: 700,
                        cursor: "pointer",
                        boxShadow: `0 1px 4px ${PASTEL.coral}40`,
                      }}
                    >
                      ✏️ 닉네임 설정하러 가기
                    </button>
                  </div>
                )}
              </div>
            );
          })}
        </div>
      )}

      {/* ======== ONLINE USERS BAR ======== */}
      <div style={{ flexShrink: 0, padding: "8px 16px", borderBottom: `1px solid ${theme.border}` }}>
        <div style={{ display: "flex", gap: 6, flexWrap: "wrap", marginBottom: recentCalls.length > 0 ? 6 : 0 }}>
          {/* Teacher (#24 enhanced) */}
          {teacherOnline ? (() => {
            const ts = TEACHER_STATUSES.find(s => s.id === teacherStatus) || TEACHER_STATUSES[0];
            return (
              <span style={{
                fontSize: 10, padding: "4px 10px", borderRadius: 10,
                background: `${ts.color}15`, color: ts.color, fontWeight: 700,
                display: "inline-flex", alignItems: "center", gap: 3,
              }}>
                <span style={{ fontSize: 8 }}>{ts.emoji}</span> 선생님 · {ts.label}
              </span>
            );
          })() : (
            <button onClick={() => {
              if (!hasPerm("plaza_call")) { showMsg("호출 권한이 없어요", 1500); return; }
              callUser(teacherName);
              showMsg(`${teacherName}을(를) 호출했어요!`, 2000);
              if ("Notification" in window && Notification.permission === "default") Notification.requestPermission();
            }} style={{
              fontSize: 10, padding: "4px 10px", borderRadius: 10, border: `1px dashed ${PASTEL.coral}`,
              background: `${PASTEL.coral}08`, color: PASTEL.coral, fontWeight: 700, cursor: "pointer",
            }}>
              📢 선생님 호출
            </button>
          )}
          {/* Other online users with avatar (#11) */}
          {onlineUsers.filter(u => u.role !== "admin").map((u, i) => {
            const avatar = u.avatar || (() => { try { return localStorage.getItem("ar_avatar_" + u.name) || ""; } catch { return ""; } })();
            const status = u.status || (() => { try { return localStorage.getItem("ar_status_" + u.name) || ""; } catch { return ""; } })();
            return (
              <button key={i} onClick={(e) => {
                // Show context menu: call or praise
                if (!hasPerm("plaza_call") && userRole !== "admin") return;
                // Simple: toggle between call and praise
                if (e.shiftKey || e.detail > 1) { // Double tap or shift = praise
                  setPanel("praise:" + u.name);
                } else {
                  callUser(u.name);
                  showMsg(`${u.name}님을 호출했어요!`, 1500);
                }
              }} onContextMenu={(e) => {
                e.preventDefault();
                setPanel("praise:" + u.name);
              }}
              title={status ? `${u.name} · ${status}` : u.name}
              style={{
                fontSize: 10, padding: "4px 10px", borderRadius: 10, border: "none", cursor: "pointer",
                background: `${roleColors[u.role] || theme.textSec}15`,
                color: roleColors[u.role] || theme.textSec, fontWeight: 600,
                display: "inline-flex", alignItems: "center", gap: 3,
              }}>
                {avatar && <span style={{ fontSize: 12 }}>{avatar}</span>}
                <span style={{ display: "inline-block", width: 5, height: 5, borderRadius: 3, background: PASTEL.mint }} />
                {u.name}
                {status && <span style={{ fontSize: 8, opacity: 0.7, maxWidth: 50, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{status}</span>}
              </button>
            );
          })}
        </div>
        {recentCalls.length > 0 && (
          <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
            {recentCalls.map((c, i) => (
              <span key={i} style={{ fontSize: 9, color: PASTEL.coral, background: `${PASTEL.coral}08`, padding: "2px 6px", borderRadius: 6 }}>
                {c.from} → {c.to}
              </span>
            ))}
          </div>
        )}
      </div>

      {/* ======== CHAT AREA ======== */}
      <div className="plaza-content" style={{ flex: 1, overflowY: "auto", padding: "12px 16px", WebkitOverflowScrolling: "touch" }}>
        {visibleLog.length === 0 && (
          <p style={{ textAlign: "center", color: theme.textSec, fontSize: 13, marginTop: 40 }}>
            아직 대화가 없어요. 첫 메시지를 보내보세요!
            <br /><span style={{ fontSize: 10 }}>메시지는 10분 후 자동 삭제됩니다 (공지는 상단에 유지)</span>
          </p>
        )}
        {visibleLog.map((msg, i) => {
          const isMe = msg.user === myName;
          const remaining = Math.max(0, Math.ceil((10 * 60 * 1000 - (now - msg.time)) / 60000));
          const reactions = msg.reactions || {};
          const reactionEntries = Object.entries(reactions).filter(([, arr]) => arr?.length > 0);

          return (
            <div key={`${msg.time}-${i}`} style={{
              display: "flex", flexDirection: isMe ? "row-reverse" : "row",
              marginBottom: 10, animation: "fadeIn 0.3s ease",
            }}>
              {/* Avatar (#11) */}
              {!isMe && (
                <div style={{ fontSize: 16, marginRight: 6, marginTop: 2, flexShrink: 0 }}>
                  {(() => {
                    const a = (() => { try { return localStorage.getItem("ar_avatar_" + msg.user) || ""; } catch { return ""; } })();
                    return a || ROLES[msg.role]?.charAt(0) || "💬";
                  })()}
                </div>
              )}

              <div style={{ maxWidth: "78%", minWidth: 0 }}>
                {/* Reply reference (#6) */}
                {msg.replyTo && (
                  <div style={{
                    fontSize: 10, color: theme.textSec, padding: "2px 8px", marginBottom: 2,
                    borderLeft: `2px solid ${PASTEL.lavender}`, marginLeft: isMe ? "auto" : 0,
                  }}>
                    ↩ {msg.replyTo.user}: {msg.replyTo.text}
                  </div>
                )}

                <div style={{
                  padding: "10px 14px", borderRadius: 14,
                  background: msg.pinned ? `${PASTEL.yellow}20` : isMe ? `${PASTEL.coral}20` : theme.card,
                  border: `1px solid ${msg.pinned ? PASTEL.yellow : isMe ? PASTEL.coral : theme.border}`,
                }}>
                  {/* Message header */}
                  <div style={{ display: "flex", gap: 6, alignItems: "center", marginBottom: 4, flexWrap: "wrap" }}>
                    {msg.pinned && <span style={{ fontSize: 10 }}>📌</span>}
                    <span style={{
                      fontSize: 9, padding: "1px 6px", borderRadius: 4,
                      background: `${roleColors[msg.role] || theme.textSec}20`,
                      color: roleColors[msg.role] || theme.textSec,
                    }}>{ROLES[msg.role] || ""}</span>
                    <span style={{ fontSize: 11, fontWeight: 700, color: theme.text }}>{msg.user}</span>
                    <span style={{ fontSize: 9, color: theme.textSec }}>
                      {new Date(msg.time).toLocaleTimeString("ko-KR", { hour: "2-digit", minute: "2-digit" })}
                    </span>
                    {remaining <= 3 && <span style={{ fontSize: 8, color: PASTEL.coral }}>{remaining}분</span>}
                  </div>

                  {/* Content */}
                  {renderMsgContent(msg)}

                  {/* Reactions display (#1) */}
                  {reactionEntries.length > 0 && (
                    <div style={{ display: "flex", gap: 4, flexWrap: "wrap", marginTop: 6 }}>
                      {reactionEntries.map(([emoji, users]) => (
                        <button key={emoji} onClick={() => addReaction(msg.time, emoji)}
                          className="reaction-btn"
                          style={{
                            fontSize: 11, padding: "1px 6px", borderRadius: 10,
                            border: `1px solid ${users.includes(myName) ? PASTEL.coral : theme.border}`,
                            background: users.includes(myName) ? `${PASTEL.coral}15` : "transparent",
                            cursor: "pointer", color: theme.text, transition: "all 0.15s",
                          }}>
                          {emoji} {users.length}
                        </button>
                      ))}
                    </div>
                  )}

                  {/* Action buttons */}
                  <div style={{ display: "flex", gap: 4, marginTop: 4, justifyContent: isMe ? "flex-end" : "flex-start", flexWrap: "wrap" }}>
                    {/* Quick reactions (#1) */}
                    {QUICK_EMOJIS.slice(0, 4).map(e => (
                      <button key={e} onClick={() => addReaction(msg.time, e)}
                        style={{ background: "none", border: "none", fontSize: 11, cursor: "pointer", padding: "1px 2px", opacity: 0.5, transition: "opacity 0.15s" }}
                        onMouseEnter={ev => ev.target.style.opacity = 1}
                        onMouseLeave={ev => ev.target.style.opacity = 0.5}>{e}</button>
                    ))}
                    {/* Reply (#6) */}
                    <button onClick={() => setReplyTarget(msg)}
                      style={{ background: "none", border: "none", fontSize: 9, color: theme.textSec, cursor: "pointer", padding: "1px 4px" }}>↩</button>
                    {/* Admin: Pin (#3) / Delete */}
                    {(userRole === "admin" || isMe) && (
                      <button onClick={() => deleteChat(msg.time)}
                        style={{ background: "none", border: "none", color: theme.textSec, fontSize: 9, cursor: "pointer" }}>삭제</button>
                    )}
                    {userRole === "admin" && (
                      <button onClick={() => togglePin(msg.time)}
                        style={{ background: "none", border: "none", color: PASTEL.yellow, fontSize: 9, cursor: "pointer" }}>
                        {msg.pinned ? "📌해제" : "📌고정"}
                      </button>
                    )}
                  </div>
                </div>
              </div>
            </div>
          );
        })}
        <div ref={chatEndRef} />
      </div>

      {/* ======== REPLY BAR (#6) ======== */}
      {replyTarget && (
        <div style={{
          flexShrink: 0, padding: "6px 16px", borderTop: `1px solid ${PASTEL.lavender}`,
          background: `${PASTEL.lavender}10`, display: "flex", alignItems: "center", gap: 8,
        }}>
          <span style={{ fontSize: 11, color: PASTEL.lavender }}>↩</span>
          <span style={{ fontSize: 11, color: theme.text, flex: 1, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
            {replyTarget.user}: {replyTarget.text}
          </span>
          <button onClick={() => setReplyTarget(null)}
            style={{ background: "none", border: "none", fontSize: 14, color: theme.textSec, cursor: "pointer" }}>✕</button>
        </div>
      )}

      {/* ======== CHAT INPUT ======== */}
      <div style={{
        flexShrink: 0, display: "flex", gap: 8, padding: "10px 16px",
        borderTop: `1px solid ${isFrozen ? PASTEL.sky : theme.border}`, background: theme.card,
        transition: "border-color 0.3s ease",
      }}>
        <input value={chatMsg} onChange={e => setChatMsg(e.target.value)}
          onKeyDown={e => { if (e.key === "Enter") { e.preventDefault(); sendChatWithReply(); } }}
          placeholder={isFrozen && userRole !== "admin" ? "🧊 광장이 얼어있어요..." : "메시지 ($수식$ 지원)"}
          disabled={isFrozen && userRole !== "admin"}
          autoComplete="off" autoCorrect="off"
          style={{
            flex: 1, padding: "10px 14px", borderRadius: 12,
            border: `1.5px solid ${isFrozen ? PASTEL.sky : theme.border}`,
            background: isFrozen && userRole !== "admin" ? `${PASTEL.sky}08` : theme.bg,
            color: theme.text, fontSize: 13, fontFamily: "'Noto Serif KR', serif",
            WebkitUserSelect: "text", userSelect: "text",
            transition: "all 0.3s ease",
          }} />
        <button onClick={sendChatWithReply}
          disabled={isFrozen && userRole !== "admin"}
          style={{
            padding: "10px 18px", borderRadius: 12, border: "none",
            background: isFrozen && userRole !== "admin"
              ? `${PASTEL.sky}30`
              : `linear-gradient(135deg, ${PASTEL.coral}, ${PASTEL.dustyRose})`,
            color: "white", fontSize: 13, fontWeight: 700,
            cursor: isFrozen && userRole !== "admin" ? "default" : "pointer",
            transition: "all 0.3s ease",
          }}>{isFrozen && userRole !== "admin" ? "🧊" : "전송"}</button>
      </div>
    </div>
  );
}

// ============================================================
// STYLE HELPERS
// ============================================================

function toolBtnStyle(theme) {
  return {
    padding: "5px 10px", borderRadius: 8, border: `1px solid ${theme.border}`,
    background: theme.card, fontSize: 11, cursor: "pointer", color: theme.text,
    transition: "all 0.15s",
  };
}

function subPanelStyle(theme) {
  return {
    flexShrink: 0, padding: "12px 16px", borderBottom: `1px solid ${theme.border}`,
    background: theme.card, animation: "fadeIn 0.2s ease", maxHeight: 300, overflowY: "auto",
  };
}

function inputStyle(theme) {
  return {
    width: "100%", padding: "8px 12px", borderRadius: 8, border: `1px solid ${theme.border}`,
    background: theme.bg, color: theme.text, fontSize: 12, fontFamily: "'Noto Serif KR', serif",
    boxSizing: "border-box",
  };
}

function actionBtnStyle(color) {
  return {
    padding: "6px 14px", borderRadius: 8, border: "none",
    background: `${color}20`, color: color, fontSize: 11, fontWeight: 700,
    cursor: "pointer", transition: "all 0.15s",
  };
}
